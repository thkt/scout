# ADR Drift Scan: 2026-05-14

`/audit-adr-drift` 3rd dogfooding。PR #94 (commit 6f2702e) で `ErrorCode` 5 → 9 variant 拡張、`ScoutError::internal` → `io_error` rename、`internal_bug` / `unknown` / `timeout` constructor 追加、`*Error::Api { 4xx }` を IoError(74) → DataError(65) に再分類。これにより ADR-0002 / ADR-0003 が実装に遅れていないかを検証。

Reviewer: `reviewer-rust` (Rust idiom angle) + `reviewer-design` (module-interface angle) parallel。

## Summary

| Metric            | Value |
| ----------------- | ----- |
| ADRs scanned      | 4 (local 0001–0004) |
| Drift findings    | 4 (after dedup) |
| H priority        | 2 |
| M priority        | 1 |
| L priority        | 1 |
| Unverifiable ADRs | 0 |
| New external ADR refs | 0 (ADR-0065 は 5/13 audit で deliberately active として認識済み) |

## Per-ADR Findings

### ADR-0001: SSRF Defense Architecture and fetch.rs Module Structure

Status: Accepted

**No drift.** Dual-client contract intact:

- `src/tools.rs:129,132`: `http` (default `Policy::limited(5)`) + `fetch_http` (`Policy::none()`) フィールド両方残存
- `src/tools.rs:141-152`: コンストラクタが redirect policy の違いを強制
- callsite usage rule: `fetch_http` for user-URL paths (`:233, :297`)、`http` for trusted GitHub/Gemini/Slack (`:166, :264`) — ADR §Implementation Guidelines と機械的に一致
- `src/fetch.rs:379, :542` の ADR-0001 ref 現在の状態を正しく示す

PR #94 はこの module に触れていない。

### ADR-0002: Adopt sysexits.h Exit Code Convention for CLI

Status: Accepted

| # | File:Line | Description | Direction | Priority |
| - | --------- | ----------- | --------- | -------- |
| 1 | `docs/decisions/0002-...md:30-39, :53, :94-102, Reassessment Trigger row` | Decision table が 5 codes (64/65/66/74/75) のみ列挙。コードは 9 codes (70/104/124 追加) を ship 済み。`src/lib.rs:244-269` T-H000 が `--help` に 8 非-zero codes (64,65,66,70,74,75,104,124) 含むことを assert し、tests T-ER025/026/027 (`src/tools/errors.rs:601-779`) が 70/104/124 を lock。code / `--help` / test の三角は内部整合、ADR のみ遅れ。Confirmation セクションと "採用 code 詳細" table、"6 つの code に収まらない" Reassessment Trigger も同じ修正対象。 | adr-update — Decision table を 8 行に拡張 + ADR-0065 9-code policy 採用の Note (date: 2026-05-14, ref: PR #94) | **H** |

### ADR-0003: Error Classification Contract for sysexits and JSON Output

Status: Accepted

| # | File:Line | Description | Direction | Priority |
| - | --------- | ----------- | --------- | -------- |
| 2 | `docs/decisions/0003-...md:102` (Implementation Guidelines) | "`internal()` への fold off は priority 5 (scout-side invariant violation) と Unknown 退避にのみ使用する" — `internal()` は PR #94 で削除済み。今は `internal_bug()` (Internal=70 / scout-side schema bug) と `io_error()` (IoError=74 / external tool failure) に分離、`unknown()` (Unknown=104 退避) と並ぶ三項。`src/tools/errors.rs:57-77` 参照。 | adr-update — `internal()` を `internal_bug()` に置換 + 70/74 sibling split の説明追加 | **H** |
| 3 | `docs/decisions/0003-...md:100, :117` (Implementation Guidelines + 参照) | "`unwrap_or_note` は `unwrap_or_degraded` (仮称) に rename" 文言 + `src/tools/errors.rs:225, :266` の "違反箇所" line cite。Rename は ADR-0003 自体の implementation で完了済み (`src/tools/errors.rs:350` に `unwrap_or_degraded` 実体、`src/tools.rs:18, :483-485` で使用)。ADR が "future work" 時制で残り、line cite も既に違反箇所ではなくなった。 | adr-update — "違反箇所" を "implementation site" に変更 + line refs を最新化または削除 (descriptive cite であり contract ではない) | M |
| 4 | `docs/decisions/0003-...md:101` (Implementation Guidelines) | "`DegradedReason` の variants は `repo_overview` 等の現実の failure mode を反映 (4-6 variant 程度を想定)"。実装は 8 variants (`src/envelope.rs:15-24`)。ADR line 57 の Note ですでに "8 種類" と開示済み (post-implementation note)、line 101 のみ residual staleness。 | adr-update — line 101 の "4-6 variant 程度を想定" を "8 variants (post-implementation, see Note 2026-05-13)" に書き換え、または line 101 削除 | L |

### ADR-0004: GitHub Client Behavioral Limits

Status: Accepted

**No drift.** PR #94 はこの module に触れていない。

- Rule 1 (403 + `x-ratelimit-remaining` 不在 → RateLimited): `src/github.rs:157-180` 維持、コメントに ADR-0004 ref
- Rule 2 (per_page > 100 → `InvalidPerPage` error): `src/github.rs:289-298` の `validate_per_page()` を issues/pulls/releases の 3 endpoint が呼ぶ
- Rule 3 (glob は repo-relative full path に match): `src/github/helpers.rs:177-181` の `m.is_match(&e.path)`

## External ADR Dependencies

| # | File:Line | External ADR ref | Recommended action |
| - | --------- | ---------------- | ------------------ |
| - | (前回 5/13 audit で文書化済み) | ADR-0065 (`~/.claude/docs/decisions/`) | 維持 — ADR-0002 line 92 で JSON schema portion を deliberately ADR-0065 active 状態として carve out。code refs (`src/tools/errors.rs:66,72,84,...`, `src/lib.rs:112,125`, `tests/cli_integration.rs:25,33,...`) はこの除外規定の対象 |

**新規 external ADR refs: なし。** ADR-0060/0066 のコード refs は無し (commit message のみ)。

## Follow-up Issue Candidates

H-priority drift (どちらも `adr-update` 方向、code-fix 不要):

- [ ] **ADR-0002 update**: Decision table を 8 codes に拡張 (`70 Internal` / `104 Unknown` / `124 Timeout` 追加)。Confirmation、採用 code 詳細、Reassessment Trigger も同期。Note: "Expanded to 9 codes per PR #94 (2026-05-14)" を末尾追記
- [ ] **ADR-0003 update**: Implementation Guidelines line 102 の `internal()` ref を `internal_bug()` に修正 + 70/74 sibling split を1段落で追記
- [ ] (M, optional) ADR-0003 cleanup: `unwrap_or_note` rename 完了済みの旨を時制と line refs に反映
- [ ] (L, optional) ADR-0003 line 101: "4-6 variant 程度を想定" を post-impl note に統合

これら全部 `code-fix` が不要 (実装は ADR 後継方針と整合)、純粋に ADR 文書の追従。
