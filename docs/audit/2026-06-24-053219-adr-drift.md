# ADR Drift Scan: 2026-06-24-053219

## Summary

| Metric            | Value |
| ----------------- | ----- |
| ADRs scanned      | 22    |
| Drift findings    | 18    |
| H priority        | 0     |
| M priority        | 2     |
| L priority        | 16    |
| Unverifiable ADRs | 0     |

All 22 ADRs are Accepted (none Superseded). Zero `code-fix` drift: every behavioral decision is implemented as decided. All findings are `adr-update`/`accept` — stale line/file references in Confirmation/References sections after code moved, two stale type/signature descriptions, and one stale Consequences note that a follow-up task has since closed.

## Per-ADR Findings

### ADR 0001: SSRF Defense Architecture and fetch.rs Module Structure

Status: Accepted

No drift detected. `ValidatedUrl` (src/fetch/ssrf.rs:89), `ssrf_check` (110), `validate_url_sync` (143), `is_blocked_host`/`is_cgn`/`is_private_ip` (155/171/176), redirect `Policy::none()` (src/tools/builder.rs:70), `download(&ValidatedUrl)` (src/fetch/download.rs:22), CDP `EventRequestPaused` listener (src/fetch/cdp.rs:264), `check_browser_request` scheme allowlist (src/fetch/cdp/launch.rs:85-95) all match the cited claims and line ranges.

### ADR 0002: Adopt sysexits.h Exit Code Convention for CLI

Status: Accepted

| #   | File:Line                               | Description                                                                                                                                                                                                                                                       | Direction  | Priority |
| --- | --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | src/signals.rs:18,20 / src/lib.rs:62-74 | Exit codes 130 (SIGINT) and 143 (SIGTERM) are emitted, shown in `--help` after_help, and pinned by T-H000 (lib.rs:416), but the ADR's exit-code table and "9 codes" inventory omit both. The ADR declares exit codes public API and anticipated this update path. | adr-update | M        |
| 2   | src/lib.rs:395-439                      | §References cites T-H000 (`root_help_contains_exit_codes_and_environment`) at lib.rs:224-270; the test now lives at lib.rs:395-439. Stale line reference; contract intact.                                                                                        | adr-update | L        |

Note: the `ErrorCode`→exit_code mapping (src/envelope.rs:200-207) matches the ADR table exactly (64/65/66/70/74/75/104/124). The 130/143 gap is on the separate signal axis.

### ADR 0003: Error Classification Contract for sysexits and JSON Output

Status: Accepted

| #   | File:Line                                        | Description                                                                                                                                                                                                                                                          | Direction  | Priority |
| --- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | src/slack.rs:56-91 / src/github/errors.rs:58-100 | §Implementation Guidelines locates the priority-ordered (`// Priority N`) arms in "each `From<...>` impl's match arm." They now live in per-error `classify()` methods; the `From` impls delegate. Priority ordering intact; only the location description is stale. | adr-update | L        |

Note: `DegradedReason` (13 variants, src/envelope.rs:15-29), `label()` (39-55), `Degradation::push` (70) all match.

### ADR 0004: GitHub Client Behavioral Limits

Status: Accepted

| #   | File:Line                     | Description                                                                                                           | Direction  | Priority |
| --- | ----------------------------- | --------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | src/github.rs:185-211         | §References cites Rule 1 (403 rate-limit fallback) at github.rs:153-165; actual 185-211. Stale ref; contract intact.  | adr-update | L        |
| 2   | src/github.rs:323-335         | Cites `PerPage::new` (`assert!(1..=100)`) at 289-298; actual `PerPage(u8)` struct at 323, `new` at 329. Bound intact. | adr-update | L        |
| 3   | src/github.rs:281,293,305     | Cites `get_issues`/`get_pulls`/`get_releases` at 316/328/340; actual 281/293/305. Fixed query (no `?page=`) intact.   | adr-update | L        |
| 4   | src/github/helpers.rs:153-181 | Cites Rule 3 (`filter_tree_entries` path-scoped glob) at 161-177; actual 153-181. Contract intact.                    | adr-update | L        |

Note: all four are stale §References line numbers from code movement; every behavioral claim (403 fallback, PerPage 1..=100, fixed sort, no pagination, `is_retriable` 500..=599 at github.rs:354) verified intact.

### ADR 0005: Switch Search Backend from Gemini Grounding to Brave Search API

Status: Accepted

No drift detected. `Lang::to_brave_param` (src/search/lang.rs:17-19) maps Ja→`Some("ja")`, En→`Some("en")`, Auto→`None`, replacing the prior `apply_to_query` per the ADR. The ADR's Implementation Status note self-discloses superseded interim refs.

### ADR 0006: Centralize Retry-After Delay in Retry Helper

Status: Accepted

| #   | File:Line                    | Description                                                                                                                                                                                                                                                    | Direction  | Priority |
| --- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | src/tools/errors.rs:43,49,56 | §More Information names the transient helpers `transient_with_retry_hint`/`transient_with_network_hint`/`timeout_with_retry_hint`; implemented as `transient_retry`/`transient_network`/`timeout_retry`. Dedup goal achieved; helper names in prose are stale. | adr-update | L        |

Note: `retry_with_rate_limit` (src/retry.rs:207) is the single centralized helper (brave/client.rs:337, github.rs:145, slack/client.rs:183). No `*_delay` duplicate helpers remain.

### ADR 0007: Unify Brave Client Init via Factory and Result-based HTTPS Check

Status: Accepted

| #   | File:Line                                   | Description                                                                                                                                                                                                                                                | Direction  | Priority |
| --- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | src/redacted.rs:37                          | Decision states `assert_https` → `validate_https(&str) -> Result<(), BraveError>`. Actual is generic `validate_https<E>(url: &str, err: impl FnOnce() -> E) -> Result<(), E>` so each client supplies its own error. Intent holds; stated signature stale. | adr-update | L        |
| 2   | src/brave/client.rs:164, src/redacted.rs:36 | Bypass moved from `skip_https_check`-only to a `should_check_https()` helper using `#[cfg(test)]`. Behavior matches (bypass set only by `with_base_url`); cosmetic divergence.                                                                             | accept     | L        |

Core decision (factory `from_env_with`, Result-based check, per-client `skip_https_check` set only by `with_base_url`) implemented as decided.

### ADR 0008: Trait Injection and ScoutBuilder for Test Seam

Status: Accepted

| #   | File:Line                    | Description                                                                                                                                                                                                                   | Direction  | Priority |
| --- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | src/tools/builder.rs:225     | Confirmation states the single `Scout { }` struct literal lives at tools.rs:701 inside `ScoutBuilder::build`. `ScoutBuilder` was extracted to src/tools/builder.rs; literal now at builder.rs:225. Decision holds; ref stale. | adr-update | L        |
| 2   | src/tools/builder.rs:130-142 | References `with_clock`/`with_rng`/`with_token_source` at tools.rs:665-681; now at builder.rs:129-189. Stale location.                                                                                                        | adr-update | L        |

Core decision verified: `Clock`/`Rng`/`TokenSource` traits in dedicated modules with named impls; `Scout` holds `Arc<dyn …>`; `Scout::new()` delegates to `ScoutBuilder::from_env()?.build()`.

### ADR 0009: Object-safe DnsResolver and Arc Injection

Status: Accepted

| #   | File:Line                | Description                                                                                                                                                                                | Direction  | Priority |
| --- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------- |
| 1   | src/fetch/ssrf.rs:279    | Decision states the test double is `FailingDnsResolver(FetchError)`. Actual `FailingDnsResolver(pub String)` wraps into `FetchError::DnsResolution`. Behavior preserved; field type stale. | adr-update | L        |
| 2   | src/tools/builder.rs:148 | Step 4 specifies `with_dns` at tools.rs:665-681; implemented at builder.rs:148. Stale location.                                                                                            | adr-update | L        |

Core decision verified: object-safe `DnsResolver` trait (ssrf.rs:21-27), `Scout.dns: Arc<dyn DnsResolver>`, `Clone` bound removed (0 hits of `Clone::clone(resolver)`).

### ADR 0010: Scout-local JSON Envelope Contract

Status: Accepted

No drift detected. All four rules verified in src/envelope.rs: `is_retryable()` = `TempFailure | Timeout` (215); `ErrorPayload` (247-255) always-emit `code`/`message`/`retryable`, skip-if-none `next_step`, skip-if-empty `candidates`; `SuccessEnvelope` (222-228) always-`[]` `notes`, skip-if-empty `degraded_reasons`; array-`[]`-never-`null` invariant upheld. ADR-0065→ADR-0010 ref migration complete (0 hits of `ADR-0065` in src/tests).

### ADR 0011: Scout-local Classification Priority Policy

Status: Accepted

No drift detected. Priority arm order verified in src/brave/client.rs::classify (82-110): UsageError(1) → DataError(2) → Timeout/TempFailure(4) → Internal(5) → `Api{4xx}` guard before bare `Api{..}` Unknown fallback. Doc comment (78-80) marks arm order load-bearing, matching the ADR's "上から順、最初にマッチで確定" rule. `IoError` via constructor, separate axis.

### ADR 0012: Connect-time IP Guard for SSRF DNS Rebinding

Status: Accepted

| #   | File:Line               | Description                                                                                                                                                                                                                  | Direction  | Priority |
| --- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | src/tools/builder.rs:73 | Decision states `SsrfResolver` is injected into `fetch_http` via `dns_resolver`. Actual injection at the shared HTTP-client build site builder.rs:73, not inside a `fetch_http` fn. Mechanism matches; named site imprecise. | adr-update | L        |

Core decision verified: `SsrfResolver` implements `reqwest::dns::Resolve` (ssrf.rs:233), rejects private IPs at connect (242-244); `ssrf_check` pre-flight retained; `InternalHost`→DataError/exit 65 (fetch.rs:102). Stateless per chosen approach.

### ADR 0013: Charset Detection and Decode Policy

Status: Accepted

No drift detected. Verified against src/charset.rs:8-20 (`is_reliable_detection` gates to exactly 8 multi-byte encodings), src/fetch/download.rs:103-193 (label-first decode, lossy fallback sets `uncertain=true`, chardetng with `Iso2022JpDetection::Allow`), src/github/encoding.rs:60-161 (hint → BOM → null-byte → reliability-gated detection → AssumedUtf8 → NonUtf8). Semantics match.

### ADR 0014: Output-Injection Defense for Agent Consumers

Status: Accepted

| #   | File:Line            | Description                                                                                                                                                                               | Direction | Priority |
| --- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | -------- |
| 1   | src/slack.rs:353-393 | ADR cites the converter-helper reuse site as slack.rs:339-382; actual `escape_yaml`/`neutralize_yaml_markers` reuse at 353-393. Cited range overlaps the real one (imprecise, not false). | accept    | L        |

Core decision verified: src/markdown.rs (`md_link` scheme allowlist + inert fallback, escape helpers, `shift_headings`), src/fetch/converter.rs:112-165 (`neutralize_yaml_markers`, `escape_yaml`). Named crates present: `dom_smoothie` 0.18, `fast_html2md` 0.0.62; `html2md::rewrite_html` at converter.rs:73. T-SE010 confirms `javascript:` not emitted as clickable.

### ADR 0015: Redacted Mandatory Secret Carrier

Status: Accepted

| #   | File:Line                   | Description                                                                                                                                                                                                                                                                             | Direction  | Priority |
| --- | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | src/slack/client.rs:137-138 | Consequences claims the `xoxp-` prefix "は error 文言のみで強制されない (別タスクで追跡)". Code now enforces it: `from_env_with` returns `SlackError::TokenWrongType` on prefix mismatch (USER_TOKEN_PREFIX line 77). Landed in 2c6708d (#261). Remove the stale "別タスクで追跡" note. | adr-update | M        |

Rest verified: src/redacted.rs:6-30 (`Redacted(String)`, `new` trims/rejects empty, `expose()`, Debug `[REDACTED]`, no Display/Deref/Serialize). Wrapping sites at token_source.rs:47,54, brave/client.rs:164,173, slack/client.rs:127. "包み忘れは型で検出できず" and "Clone は zeroize 無し" remain true.

### ADR 0016: GitHub Formatter Output Schema

Status: Accepted

| #   | File:Line              | Description                                                                                                                                                                                                              | Direction  | Priority |
| --- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | -------- |
| 1   | src/github/types.rs:72 | "More Information" cites a dangling `ADR-0065` source comment as the repoint target. That comment was already repointed to `#67/ADR-0010` (445ff57), so the ADR's note about an outstanding ADR-0065 reference is stale. | adr-update | L        |

Decision verified: src/github/format.rs — `MAX_README_BYTES = 24_000` (6), deterministic order `format_overview` → `format_metadata_table` → `format_readme_section` (153-174) with UTF-8-safe truncation, newline snap, `shift_headings(…, 2)`, truncation marker.

### ADR 0017: Signal Exit Codes and Graceful Drain

Status: Accepted

No drift detected. Verified: src/signals.rs:3-101 (`InterruptSignal::{Sigint, Sigterm}`, Sigterm `#[cfg(unix)]`, `exit_code()` → 130/143, `wait_for_signal` races ctrl_c + SIGTERM) and src/lib.rs (`SHUTDOWN_DRAIN_TIMEOUT = 7s` line 37, `drive` at 128 runs select → cancel → timed drain → `Outcome::Interrupted`). T-S001/S002/S005/S006, T-DRV001/002/003 pin these.

### ADR 0018: GitHub Token Resolution Precedence

Status: Accepted

No drift detected. `resolve_from_env_or_gh` (src/token_source.rs:47-93) reads `["GITHUB_TOKEN","GH_TOKEN"]` in order via `Redacted::new`, falls to `gh auth token` with `kill_on_drop(true)`, `TOKEN_RESOLVE_TIMEOUT = 5s` (15), stdout-only, stderr withheld on non-zero exit. github.rs:41 comment matches order; "60 req/hour" hint at github.rs:77.

### ADR 0019: Env-Var Validation and Timeout Hierarchy

Status: Accepted

No drift detected. Defaults 95/45/60/180s, retries 2, ranges `TIMEOUT_MIN_SECS=1`/`MAX=600`, `RETRIES_CAP=10` (src/tools/config.rs:10-39); `from_env_with` fails range/parse via `user_error` (exit 64), NotPresent→default, NotUnicode→usage error; `surface_overrides` emits one `info!` per override. after_help (lib.rs:82-91) mirrors ranges and 180s > inner timeouts.

### ADR 0020: Search Default Output One URL Per Line

Status: Accepted

No drift detected. `Scout::search` (src/tools/query.rs:20-44) joins `s.url.as_str()` with `\n`, returns `CommandOutput::ok(markdown, data)` with `data = {query, sources}`; comment 31-32 matches ADR; empty output preserved (lib.rs:44-54).

### ADR 0021: CDP Chromium Launch Egress Flags

Status: Accepted

No drift detected. `build_launch_args` (src/fetch/cdp/launch.rs:67-83) contains exactly the ADR's hardening group (`--headless=new`, `--disable-webrtc`, `--disable-background-networking`, `--disable-features=DnsOverHttps`, `--disable-domain-reliability`, `--no-pings`, `--disable-extensions`, `--no-first-run`, `--disable-default-apps`) and proxy group (`--proxy-server=socks5://127.0.0.1:{proxy_port}`, `--proxy-bypass-list=<-loopback>`, `--disable-quic`). T-F043/T-201-8 present. The dangling spec.md reference the ADR replaces is gone (launch.rs:56 → ADR-0021).

### ADR 0022: Slack User-Token Prefix Enforced at Construction

Status: Accepted

No drift detected. `USER_TOKEN_PREFIX = "xoxp-"` (src/slack/client.rs:77); `from_env_with` validates `starts_with` after `Redacted::new`, returns `SlackError::TokenWrongType` on mismatch (135-139); `classify()` folds `TokenNotSet | TokenWrongType` into `UsageError` + xoxp hint (src/slack.rs:59-60); after_help (lib.rs:79) states the same contract.

## External ADR Dependencies

References to `ADR-0060`/`ADR-0061`/`ADR-0065` exist (94 hits) but only in non-source artifacts: `.claude/workspace/` planning & research notes and prior `docs/audit/` reports. No source or test file references them (ADR-0010/0011/0012 confirm 0 `ADR-0065` hits in src/tests). These are historical planning ids never promoted to scout-local ADRs.

| #   | File:Line                                            | External ADR ref    | Recommended action                                                                           |
| --- | ---------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------- |
| 1   | .claude/workspace/research/2026-05-07-…cli-gap.md:1+ | ADR-0060 / ADR-0061 | Planning-only ids; no promotion needed unless a decision still lacks a record (use /census). |
| 2   | .claude/workspace/planning/2026-05-07/idr-0\*.md     | ADR-0065            | Superseded by local ADR-0010 (JSON envelope) + ADR-0011 (classification). No action.         |

## Follow-up Issue Candidates

No H-priority findings. The two M-priority items are documentation corrections to the ADR bodies themselves (hand to `/adr`), not code work:

- [ ] ADR 0002 at src/signals.rs:18,20: add exit codes 130/143 to the exit-code table and update the "9 codes" inventory.
- [ ] ADR 0015 at src/slack/client.rs:137-138: remove the stale "別タスクで追跡" Consequences note; xoxp- prefix is now enforced at construction (ADR-0022).
