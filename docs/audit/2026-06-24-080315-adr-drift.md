# ADR Drift Scan: 2026-06-24-080315

22 ADR (全て `accepted`、superseded なし) の Decision Outcome を現行コードに照合した。`code-fix` 級ドリフト (コードが正しい contract から逸脱) はゼロ。検出された 4 件は全て `adr-update` / `accept` で、コードが現状の真実源であり ADR 側の文言・参照が陳腐化したケースのみ。直近 commit (`a61fb6b` 系の ADR drift fix) で本文側は再整合済み。本 PR で ADR-0011 の検証 recipe、ADR-0009 の dangling row 注記、ADR-0014 の Test-ID 衝突を解消し、historical スナップショットの line ref (ADR-0011 #2) は decline とした。

## Summary

| Metric            | Value |
| ----------------- | ----- |
| ADRs scanned      | 22    |
| Drift findings    | 4     |
| H priority        | 0     |
| M priority        | 1     |
| L priority        | 3     |
| Unverifiable ADRs | 0     |

## Per-ADR Findings

ドリフトのない ADR は一覧末尾にまとめる。以下は finding を持つ ADR のみ。

### ADR 0009: Object-Safe DNS Resolver and Arc Injection

Status: Accepted

| #   | File:Line   | Description                                                                                                                                                                      | Direction  | Priority |
| --- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | (NOT FOUND) | ヘルパ表の `engine::fetch_one` は refactor で消滅。SSRF pre-fetch read は `fetch_page` (src/fetch.rs:150) にインライン化され、resolver 注入の挙動は保持。表の行が dead reference | adr-update | L        |

`DnsResolver` trait (`src/fetch/ssrf.rs:26`)、`DnsLookupFuture` (`:21`)、各 fetch path の `resolver: Arc<dyn DnsResolver>` 引き回し、step 5 の「tools.rs に `TokioDnsResolver` literal なし」は全て現行コードで成立。ドリフトは表 1 行のシンボル名のみ。

### ADR 0011: scout-local Classification Priority Policy

Status: Accepted

| #   | File:Line               | Description                                                                                                                                                                                                                                                                                                             | Direction  | Priority |
| --- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | src/tools/errors.rs     | Confirmation の検証手段である grep 文字列 `// per ADR-0011 priority N` が現行コードに 0 hit。実際の priority コメントは各 backend の `classify()` 側 (`src/brave/client.rs:81`, `src/github/errors.rs:58`, `src/slack.rs:56`, `src/fetch.rs:93`) に `// Priority N:` として存在。ADR の検証 recipe が陳腐化し空振りする | adr-update | M        |
| 2   | src/tools/errors.rs:713 | Context の line ref が errors.rs:713 まで言及するが、現ファイルは 245 行。ただし当該 Context は冒頭で 2026-05-19 の pre-migration スナップショットと時制を明示しており当時の行番号記録。live drift ではないため不変更 (decline)                                                                                         | accept     | L        |

priority ordering 自体 (5 段 + Unknown fallback) は完全に保持。test pin T-ER023/024 (`src/tools/errors/classification_tests.rs:156/181`) も present。修正対象は検証 recipe (#1) のみで、line ref (#2) は historical Context のため decline。契約は無傷。

### ADR 0014: Output-Injection Defense for Agent Consumers

Status: Accepted

| #   | File:Line                      | Description                                                                                                                                                          | Direction | Priority |
| --- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | -------- |
| 1   | src/search/engine/tests.rs:278 | Test-ID `[T-SE010]` が衝突。`:80` が ADR-0014 の injection-defense test (契約 pin)、`:278` は無関係な Brave エラー test。pin が曖昧化。`:278` を別 ID へ再採番すべき | accept    | L        |

7 つの neutralize 関数 (`escape_md_link`/`md_link`/`escape_md_inline`/`sanitize_heading`/`shift_headings` @ `src/markdown.rs`、`neutralize_yaml_markers`/`escape_yaml` @ `src/fetch/converter.rs`) は全て cited 行で present、Slack 再利用も確認。契約は成立し、finding は ID 衝突のみ。

### ADR 0021: CDP Chromium Launch Egress Flags

Status: Accepted

| #   | File:Line                  | Description                                                                                                                                                               | Direction | Priority |
| --- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | -------- |
| 1   | src/fetch/cdp/launch.rs:56 | ADR が「dangling な `spec.md` 参照」と記述するコメントは既に `See ADR-0021` へ repoint 済 (commit 445ff57)。ADR の過去形ナレーションは history であり live drift ではない | accept    | L        |

`build_launch_args` の 12 個の固定 flag (`--proxy-server=socks5://127.0.0.1:{port}`, `--proxy-bypass-list=<-loopback>`, `--disable-quic` 等)、subrequest scheme check、test pin T-F043/T-201-8 は全て一致。

### ドリフトなし (18 ADR)

以下は Decision Outcome の全シンボル・file:line・test pin が現行コードと一致し、相違は symbol が存在し挙動も記述どおりの line-number 微小シフト (accept) のみ。

| ADR  | Title                                                      |
| ---- | ---------------------------------------------------------- |
| 0001 | SSRF Defense Architecture and fetch.rs Module Structure    |
| 0002 | sysexits.h Exit Code Convention                            |
| 0003 | Error Classification Contract for sysexits and JSON Output |
| 0004 | GitHub Client Behavioral Limits                            |
| 0005 | Switch Search Backend from Gemini to Brave Search API      |
| 0006 | Centralize retry-after Delay in Retry Helper               |
| 0007 | Unify Brave Client Init via Factory and Result-based HTTPS |
| 0008 | Trait Injection and ScoutBuilder for Test Seam             |
| 0010 | scout-local JSON Envelope Contract                         |
| 0012 | Connect-time IP Guard for SSRF DNS Rebinding               |
| 0013 | Charset Detection and Decode Policy                        |
| 0015 | Redacted Mandatory Secret Carrier                          |
| 0016 | GitHub Formatter Output Schema                             |
| 0017 | Signal Exit Codes and Graceful Drain                       |
| 0018 | GitHub Token Resolution Precedence                         |
| 0019 | Env-Var Validation and Timeout Hierarchy                   |
| 0020 | Search Default Output: One URL Per Line                    |
| 0022 | Slack User-Token Prefix Enforced at Construction           |

## External ADR Dependencies

`docs/decisions/` にローカル実体を持たない `ADR-NNNN` 参照。共有 dotclaude 設定側の meta ADR とみられ、scout 本体コードではなく `.claude/workspace` / `docs/` 配下からのみ参照される。

| #   | File:Line                                                          | External ADR ref                 | Recommended action                                    |
| --- | ------------------------------------------------------------------ | -------------------------------- | ----------------------------------------------------- |
| 1   | .claude/workspace/planning/2026-05-18/idr-01.md:114                | ADR-0065 (not local、計 89 参照) | scout-local ADR へ promote するか、local で supersede |
| 2   | .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:1  | ADR-0060 (not local、計 12 参照) | scout-local ADR へ promote するか、local で supersede |
| 3   | .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:88 | ADR-0061 (not local、計 4 参照)  | scout-local ADR へ promote するか、local で supersede |

参照は全て `.claude/workspace` (planning/research) と `docs/audit` 配下で、`src/` のコードコメントからの参照はなし。実装契約を縛る性質ではないため優先度は低い。

## Follow-up Issue Candidates

- [x] ADR 0011 Confirmation: 検証 recipe を backend `classify()` 側 `// Priority N:` 配置に合わせて更新 (本 PR, M)。errors.rs:713 の line ref は 2026-05-19 pre-migration スナップショットの当時記録のため decline (不変更)
- [x] ADR 0009: ヘルパ表の `engine::fetch_one` 行に `fetch_page` インライン化の注記を追加 (本 PR, L)。行自体は当時の signature 変更記録として保持
- [x] ADR 0014 at src/search/engine/tests.rs:278: 衝突した `[T-SE010]` を `[T-SE012]` へ再採番 (本 PR, test hygiene)。ADR-0014 契約 pin (`:80`) は不変
