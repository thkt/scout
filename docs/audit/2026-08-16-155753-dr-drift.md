# DR Drift Scan: 2026-08-16-155753

## Summary

| Metric           | Value |
| ---------------- | ----- |
| DRs scanned      | 27    |
| Drift findings   | 105   |
| H priority       | 13    |
| M priority       | 29    |
| L priority       | 63    |
| Unverifiable DRs | 0     |

## Per-DR Findings

### DR 0001: SSRF Defense Architecture and fetch.rs Module Structure

Status: accepted

Result: 6 findings (H 0 / M 2 / L 4).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0001-ssrf-defense-architecture-and-fetchrs-module-structure.md:78 | Implementation Guidelines 表は `Scout::http` の用途を「Gemini API / GitHub API / 信頼済みエンドポイント」と記す。Gemini backend は ADR-0005 (2026-05-15 決定、2026-05-16 実装完了) で Brave Search API へ置き換えられ、`src/` 配下に Gemini への参照は grep で 0 件 (`grep -rin gemini --include=*.rs .` で src ヒット無し)。現在の `Scout::http` の実消費者は GitHub API / Slack API / Brave Search API で、これは `src/tools/builder.rs:67` のコメントが「Brave, GitHub, Slack」と正しく列挙している。ADR-0001 (2026-05-13 制定) は Gemini 除去の 2-3 日前に書かれており、その後の 2 本の Addendum (いずれも 2026-06-24) もこの表の記載更新を取りこぼしている。 | dr-update | M |
| docs/decisions/0001-ssrf-defense-architecture-and-fetchrs-module-structure.md:92 | Reassessment Trigger「`fetch.rs` 行数 &gt; 2000」は split 後、二度と発火し得ない形になっている。分割後の `fetch.rs` は 462 行のままだが、split で移った実装量 (converter.rs 実装部 1017 行 [3177 行中 1018 行目から `mod tests`]、cdp.rs 335 行、cdp/launch.rs 289 行、cdp/proxy.rs 182 行、cdp/proxy/transport.rs 114 行、download.rs 241 行、extractor.rs 実装部 156 行、ssrf.rs 実装部 331 行を合算すると約 3127 行) は trigger の想定値 2000 行を既に超えている。2026-06-24 Addendum の「いずれも発火していない」(同ファイル 140 行目) は単一ファイル `fetch.rs` について文字どおり正しいが、trigger が計測対象としていた「module 全体の実装量」という意図は、split でファイルが複数に分かれた時点で `fetch.rs` という単一ファイル名では二度と計測できなくなっている。Trigger の分母を module 合算行数か主要ファイル (converter.rs 等) 個別の閾値へ更新すべき。 | dr-update | M |
| docs/decisions/0001-ssrf-defense-architecture-and-fetchrs-module-structure.md:38 | Consequences の「Bad, because `fetch.rs` の `allow(dead_code)` smell は残る」は、smell の所在地が split で移動した事実を反映していない。現在 `fetch.rs` 自体に `allow(dead_code)` は 0 件で、実体は `src/fetch/cdp.rs:8,38,53` と `src/fetch/cdp/launch.rs:95,259` の計 5 箇所に存在する。2026-06-24 の 2 本の Addendum は split と `ValidatedUrl` を記録したが、smell の移動先はどちらにも記録されていない。 | dr-update | L |
| docs/decisions/0001-ssrf-defense-architecture-and-fetchrs-module-structure.md:99 | 参照ファイル節は「ADR 制定時の行参照は...現行位置に更新済み」と述べつつ `src/tools/builder.rs:57-76 (build_default_clients、fetch_http に Policy::none())` と記すが、現在の該当箇所は doc comment が 62 行目、関数シグネチャが 74 行目で範囲内に収まる一方、`fetch_http` に `.redirect(Policy::none())` を設定する行は 96 行目で、引用範囲 57-76 の外にある。builder.rs は当該 ADR 更新コミット (`fa57510`) 以降も複数コミット (`2c05af0` 等のコメント整理を含む) で編集されており、行参照が再びずれている。 | dr-update | L |
| docs/decisions/0001-ssrf-defense-architecture-and-fetchrs-module-structure.md:140 | module split (`a7a7a4f`) は Addendum で既に記録済みで現行コード (`src/fetch.rs` 462 行 + `src/fetch/{ssrf,cdp,download,converter,extractor}.rs` 等への分割) と整合している。新たな drift 無し。 | accept | L |
| docs/decisions/0001-ssrf-defense-architecture-and-fetchrs-module-structure.md:144 | `ValidatedUrl` newtype (`src/fetch/ssrf.rs:88-102` 相当) の導入は Addendum で既に記録済みで、現行コードの `download` 等が `&ValidatedUrl` を要求する実装と整合している。新たな drift 無し。 | accept | L |

### DR 0002: Adopt sysexits.h Exit Code Convention for CLI

Status: accepted

Result: 4 findings (H 1 / M 0 / L 3).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0002-adopt-sysexitsh-exit-code-convention-for-cli.md:38 | DR-0002's decision table (line 38: 'EX_IOERR \| I/O エラー (network, disk, external tool failure)') and detail table (line 105: '74 \| network error、TLS error、headless browser CDP error') both state that network/TLS-level failures map to exit 74. The implementation deliberately routes them elsewhere: src/classify.rs:87-98 (`Classification::from_reqwest`) sends a `reqwest::Error` to `Timeout` (124) if `e.is_timeout()`, else to `TempFailure` (75) via `transient_network()` (classify.rs:93-95) if `is_transient_network(e)`, else to `Unknown` (104). `ErrorCode::IoError` is constructed only for headless-browser CDP failures (src/fetch.rs:155, `BrowserFailed`) and HTTP-client/proxy init failures (src/tools/builder.rs:84,101,109) — never for a network-level `reqwest::Error`. Test T-ER003 (src/tools/errors/exit_code_tests.rs:96-132) explicitly builds a connection-refused `reqwest::Error` and asserts `error_kind() == TempFailure` (exit 75), directly contradicting the DR's '74 = network error' claim. The already-enforced public contract already matches the code, not the DR: --help text (src/lib.rs:72, enforced by T-H000) reads '74 IO error (external tool failure such as headless browser)' with no mention of network, and README.md:293 repeats the same narrower wording. Code, help text, and README are mutually consistent and deliberately designed (see the load-bearing ordering comment at classify.rs:75-86 explaining why an unrecognized transport failure must not be lumped into IoError); the DR's table cells were never updated to match. A downstream CLI script trusting the DR literally would treat network failures as non-retryable IoError and skip the retry the DR's own Consequences section promises for EX_TEMPFAIL (`[ $? -eq 75 ] && retry`), inverting the retry semantics the DR exists to provide. | dr-update | H |
| docs/decisions/0002-adopt-sysexitsh-exit-code-convention-for-cli.md:52 | DR-0002's Confirmation section cites `src/lib.rs:395-451` as the T-H000 test location. The test (fn root_help_contains_exit_codes_and_environment) now lives at src/lib.rs:481-535 (doc comment at 479); five new tests (T-W004/W005/W006, plus the tests module growth) were inserted above it after the DR's 2026-05-14 post-implementation note claimed the line range was 'synced'. The check content itself (Exit codes section, sysexits.h string, all 8 non-zero codes, description strings) still matches exactly — this is a stale line pointer, not a behavioral drift. The DR's own 参照 section repeats the same stale range at line 121. | dr-update | L |
| docs/decisions/0002-adopt-sysexitsh-exit-code-convention-for-cli.md:106 | The '採用 code 詳細' table's row for exit 75 lists 'rate limit (429)、Gemini API 503、5xx (500-599)' as example call sites, but scout has no Gemini integration anywhere in src (grep for 'gemini' across src returns zero hits). scout's only backends are Brave Search, GitHub, Slack, and the fetch/CDP pipeline (confirmed via src/brave, src/github, src/slack, src/fetch). This looks like an unadapted copy from the superseded dotclaude meta-ADR (0065) this DR supersedes. It has no functional effect since the 500-599 branch in src/classify.rs:65 already covers any 5xx regardless of API name, so nothing in the code needs to change. | accept | L |
| docs/decisions/0002-adopt-sysexitsh-exit-code-convention-for-cli.md:122 | DR-0002's 参照 section cites `README.ja.md:232` for the 'sysexits.h 規約に準拠' line, but that sentence now lives at README.ja.md:273 (and the English mirror at README.md:284). Content matches the DR table verbatim (exit codes 0/64/65/66/70/74/75/104/124 plus 130/143 signal codes); only the line-number citation has decayed as the file grew. | dr-update | L |

### DR 0003: Error Classification Contract for sysexits and JSON Output

Status: accepted

Result: 7 findings (H 0 / M 5 / L 2).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0003-error-classification-contract-for-sysexits-and-json-output.md:50 | DR-0003 Partial Failure Handling bullet still specifies `reason: Option<DegradedReason>` (singular, at most one reason). Actual implementation (src/envelope.rs:15-30 DegradedReason enum, :103 `degraded_reasons: Vec<DegradedReason>`, :221 SuccessEnvelope field) is a Vec accumulating all reasons for a given command, not an Option holding one. src/tools/query.rs's fetch_slack pushes up to 4 DegradedReason values (SlackThreadTruncated/SlackUsersCapped/SlackLookupFailed/SlackOutputTruncated) into one output, which an Option&lt;DegradedReason&gt;-shaped consumer could not represent. The 2026-05-13 follow-up note does name the field `degraded_reasons` and describe it as additive, but never states it replaces the body's `reason: Option<DegradedReason>` bullet, so the DR body and its own note are inconsistent side by side. | dr-update | M |
| docs/decisions/0003-error-classification-contract-for-sysexits-and-json-output.md:73 | The README-404-silent note states the design invariant "degraded flag を『abnormal なときだけ立てる』契約に保つ", but CommandOutput::with_degradation (src/envelope.rs:118-131) sets degraded = !degradation.is_empty() unconditionally, and issue #222 (src/tools/query.rs:139-159) pushes SlackThreadTruncated / SlackUsersCapped / SlackOutputTruncated on hitting a documented, deterministic page/size cap — not a fetch failure. Hitting a cap on a long thread is expected behavior, yet it now flips the public `degraded: true` field the same way a genuine partial failure does. The DR's variant-addition notes (2026-06-17 x2) each argue their own additive-JSON-safety in isolation and never revisit whether "abnormal only" still holds once cap-hits are folded into the same boolean. A `--json` consumer branching on `degraded` alone cannot tell "a fetch failed" from "your thread was longer than the cap" without also inspecting degraded_reasons. | dr-update | M |
| docs/decisions/0003-error-classification-contract-for-sysexits-and-json-output.md:85 | Confirmation bullet requires "repo_overview partial failure path で --json 出力に degraded フィールドが含まれることの integration test". No such test exists: tests/cli_integration.rs and tests/exit_code_contract.rs have zero matches for "degraded" or "repo_overview", so no test drives the CLI end-to-end and asserts on the actual --json output. Coverage is split into two unit-level tests that never touch the wire format together: src/tools/repo.rs:447 (repo_overview_states_a_failed_section_in_the_body) asserts on the CommandOutput::degraded_reasons() test-only accessor, and src/envelope/tests.rs:302 (T-EN013) asserts on a SuccessEnvelope constructed by hand with degraded_reasons: vec![DegradedReason::IssuesFetchFailed] rather than one produced by repo_overview. Together they cover the same claim by composition, but the DR asks for an integration test and none exists at that boundary. | code-fix | M |
| docs/decisions/0003-error-classification-contract-for-sysexits-and-json-output.md:123 | Reassessment Triggers row reads "GitHub / Slack / Gemini が新規 4xx status を導入 → mapping table に row 追加". GeminiError no longer exists in src (ADR-0005, 2026-05-15, replaced Gemini grounding with BraveError; grep for Gemini across src/ returns zero hits). Meanwhile Classification::from_http_status (src/classify.rs:63) is now also delegated to by BraveError::Api/Server (src/brave/client.rs:100,110) and FetchError::Status (src/fetch/download.rs:75; src/fetch.rs:142-146), neither of which the trigger row names. The operational consequence: a new Brave or Fetch 4xx status divergence would not fire this reassessment trigger at all, while a dead backend (Gemini) still would. Same stale/incomplete list repeats at line 11 (background prose) and line 84 (Confirmation bullet: "各 error source (Slack/GitHub/Fetch/Gemini) で ... unit test" — Fetch is named but Brave is not, and Gemini still is). | dr-update | M |
| src/tools/repo.rs:446 | DR-0003 Confirmation requires "repo_overview partial failure path で --json 出力に degraded フィールドが含まれることの integration test". No single test exercises both halves together: repo_overview_states_a_failed_section_in_the_body (repo.rs:446-511) drives repo_overview through a mocked partial failure and asserts CommandOutput::degraded_reasons() plus the Markdown note, but never serializes to JSON or checks the `degraded`/`degraded_reasons` keys. success_envelope_surfaces_degradation (src/envelope/tests.rs:127-140) asserts the serialized `"degraded":true` shape, but constructs a SuccessEnvelope by hand rather than driving it from repo_overview. grep of tests/cli_integration.rs for "degraded" and "repo_overview" returns no matches, so the Confirmation item's literal target (repo_overview partial failure -&gt; --json degraded field, end to end) is not covered by any single test. | code-fix | M |
| docs/decisions/0003-error-classification-contract-for-sysexits-and-json-output.md:84 | Confirmation section still lists "Gemini" as one of the 4 error sources requiring an HTTP status -&gt; ErrorCode mapping unit test (also at line 11 Context and line 123 Reassessment Triggers). ADR-0005 (switch-search-backend-from-gemini-grounding-to-brave-search-api, 2026-05-15) removed the Gemini backend; grep of src/ confirms no GeminiError type exists. The code's actual 4-backend set is Slack/GitHub/Fetch/Brave, and T-ER034 (src/tools/errors/classification_tests.rs:156) plus src/slack/classify_tests.rs:369 already cover Slack/GitHub/Fetch/Brave, not Gemini. This DR predates or was never updated after the same repo's own ADR-0005. | dr-update | L |
| src/tools/repo.rs:257 | collect_path_candidates (used only on the repo NotFound error path to build ErrorPayload.candidates) logs-and-swallows get_repo/get_tree failures and a fetch timeout, returning Vec::new() with no degradation signal — the same shape the DR's "silent log-only fallback (unwrap_or_note 系) は廃止" line bans. This is accepted rather than flagged: the function's own doc comment (repo.rs:241-244) states it is best-effort and returns empty on any failure, it operates only inside an already-failing NotFound error response (not a successful/degraded CommandOutput), and its output (typo candidates) has no field in the success envelope's degraded/degraded_reasons contract to attach to. | accept | L |

### DR 0004: GitHub Client Behavioral Limits

Status: accepted

Result: 6 findings (H 0 / M 2 / L 4).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/tools/params.rs:143 | Rule 3 changed filter_tree_entries to match the full repo-relative path (`src/*.rs` now works), but the `--pattern` doc comment ('Glob pattern to filter filenames', example `*.rs`) and README.md:213 ('Glob pattern for filenames') both still describe the pre-fix filename-only semantics that Rule 3 explicitly deprecated ('filename-only match の旧 behavior は廃止'). A user reading `--help` or the README has no way to discover the path-scoped matching the DR introduced, and may still write filename-only globs expecting the old behavior. | code-fix | M |
| src/tools/params.rs:188 | DR 0004's Confirmation explicitly requires that `scout repo-overview --help` (and README) document that get_issues/get_pulls/get_releases surface only the first page (OVERVIEW_ITEMS=5, OVERVIEW_RELEASES=3 in src/tools/repo.rs:17-18) with no pagination and fixed filter/sort, per Rule 4/5. The `after_help` block on RepoOverviewParams here contains only Examples/Environment sections; neither it nor README.md:238 ('Repo metadata, README, open issues, PRs, and recent releases') states the first-N-only / no-pagination semantics or points callers needing full history to `gh` CLI as Rule 4 specifies. The runtime output partially self-documents ('## Recent Issues' header in src/github/format.rs:194, and the true total shown separately via '\| Open Issues \|' at format.rs:143), which is why this is not silent truncation at the point of consumption, but the DR's explicit Confirmation checklist item for README/--help documentation is unmet. | code-fix | M |
| docs/decisions/0004-github-client-behavioral-limits.md:99 | Several of DR-0004's own file:line citations no longer match current code, making them misleading pointers for anyone verifying the decision. Confirmation cites `src/github.rs:153` for the 403+missing-header unit test, but line 154 is `get_json`'s signature; the actual 403 branch is at github.rs:209-240 and the test (T-GH010) lives in src/github/http_tests.rs:150. Confirmation cites `src/github/helpers.rs:161` for the filter_tree_entries glob test, but line 161 falls inside `apply_line_range`; `filter_tree_entries` itself is at helpers.rs:175 and its test is in src/github/helpers/tests.rs:258. The Rule 4 header and Confirmation both cite `src/github.rs:316,328,340` for get_issues/get_pulls/get_releases, but they are now at 318, 330, 342 (off by 2, likely from the `PerPage` struct insertion above them at lines 355-379 predating a later edit, or an intervening doc-comment addition). Context section lines 13 and 15 carry the same stale `github.rs:153` / `helpers.rs:161` citations, though those describe pre-fix history rather than current code. | dr-update | L |
| src/github.rs:318 | DR 0004's Rule 4 and Confirmation section cite `src/github.rs:316,328,340` for get_issues/get_pulls/get_releases. Current lines are 318, 330, and 342 respectively (the fixed query strings and PerPage-only signatures the DR describes are otherwise present and correct at these lines). The DR's line pins have drifted from refactors since it was written; the behavioral contract itself matches. | dr-update | L |
| src/github/helpers.rs:175 | DR 0004's Confirmation section cites `src/github/helpers.rs:161` for the filter_tree_entries path-scoped glob test target. The function is currently at line 175 (implementation correctly matches full repo-relative path per Rule 3, verified by T-GHH020/T-GHH023 in src/github/helpers/tests.rs). Only the DR's line pin is stale. | dr-update | L |
| src/tools/repo.rs:210 | Rule 4's model treats each list endpoint call as returning 'up to per_page' items ('per_page で 1 call = 最大 100 件'), but for `get_issues` the DR omits that GitHub's issues endpoint mixes in PRs, which `repo_overview` filters out post-fetch via `real_issues` (repo.rs:210) before building the JSON `issues` field. With `per_page=OVERVIEW_ITEMS` (5), the visible issue count can be silently lower than 5 even though the DR's 'first N items' framing implies exactly up to N. The DR's per-endpoint cap model is imprecise for the issues endpoint specifically; worth a note in Rule 4 or the repo_overview doc this finding also touches. | dr-update | L |

### DR 0005: Switch Search Backend from Gemini Grounding to Brave Search API

Status: accepted

Result: 4 findings (H 1 / M 2 / L 1).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/fetch/converter.rs:25 | DR-0005's Confirmation section pins the research JSON schema as `data.fetched_pages[i] = {url, markdown, used_raw_fallback}`. In code, `FetchResult.used_raw_fallback` (and `decode_uncertain`) carry `#[serde(skip_serializing)]` (src/fetch/converter.rs:24-30), so `data.fetched_pages[i]` actually serializes to `{url, markdown}` only. The flag is instead surfaced via `degraded_reasons`/notes (src/tools/query.rs `collect_research_degradations`), a design later than DR-0005's original schema text (comment cites issue #241). No test asserts the presence of `used_raw_fallback` inside `data.fetched_pages[i]` — T-TS028 (src/tools/query_tests.rs:171) only asserts `fetched_pages` is an array, not its per-item shape, so the drift is undetected by the test suite. Since the current degradation-channel design is deliberate and documented in-code (not an accident), and downstream `jq` consumers relying on the DR's literal schema text would silently get an absent field, the DR's schema bullet is the stale artifact. | dr-update | H |
| src/fetch/converter.rs:24 | DR-0005's Decision Outcome JSON schema block (research JSON, `data.fetched_pages[]`) explicitly documents a per-page `used_raw_fallback` boolean field (`{"url": "...", "markdown": "...", "used_raw_fallback": false}`). The actual `FetchResult` struct marks `used_raw_fallback` `#[serde(skip_serializing)]` with a comment stating it is 'surfaced as a notes entry ... not as data'; `to_data_value(&report)` therefore serializes each `fetched_pages[]` entry as `{url, markdown}` only, and the flag is instead aggregated app-wide into `degraded_reasons`/`notes` (see collect_research_degradations in src/tools/query.rs). git blame shows this skip_serializing attribute was added 2026-05-07 (commit 187de1f), a week before DR-0005 was authored (2026-05-15), so the DR's JSON sketch was written without checking the pre-existing serde attribute rather than the code drifting after the fact. No test (src/tools/query_tests.rs T-TS028) asserts the field's presence either, so nothing currently enforces the DR's literal shape. README.md/README.ja.md already describe the schema as 'unchanged in shape' without naming this field, i.e. the user-facing doc already matches the code, not the DR. | dr-update | M |
| src/search/engine.rs:216 | DR-0005 Design Refinements states the non-JSON `research` output keeps only `Sources` and `Fetched Pages` sections (Search Result removed). README.md:330 and README.ja.md:319 restate the same two-section claim. In code, `format_report` (src/search/engine.rs:198-203) unconditionally calls `format_failed_urls`, which emits a third `## Failed URLs` section whenever any source fetch fails (tested at src/search/engine/tests.rs:79, `T-SE014`). This section predates the Gemini→Brave migration (git history shows it existing before DR-0005's commits) and is unrelated to the backend swap, so the DR's 'only two sections' framing is an incomplete carry-over rather than an accidental regression — the code's three-section behavior is the intended, tested contract. | dr-update | M |
| src/tools/query_tests.rs:166 | DR-0005 Confirmation requires: '統合テストで `search` コマンドが redirect URL を返さないこと (`vertexaisearch.cloud.google.com` を含まないこと) を assert'. The only test asserting absence of `vertexaisearch.cloud.google.com` is T-TS002 `research_success_returns_report` (line 166), which exercises `research`, not `search`. No test for the `search` handler literally checks for that substring. In practice this is already covered by a stronger proxy: T-TS024 `search_returns_plain_url_list` (query.rs:14) asserts markdown equals the exact Brave-returned URLs verbatim, which structurally rules out any redirect-wrapping since `search` never touches Gemini/grounding code (that module is fully removed). Treating as trivial since the named guarantee is provably satisfied by a stricter existing assertion, not a live gap in behavior. | accept | L |

### DR 0006: Centralize retry-after delay in retry helper

Status: accepted

Result: 1 finding (H 0 / M 1 / L 0).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0006-centralize-retry-after-delay-in-retry-helper.md:34 | DR-0006's Decision Outcome states each backend keeps full control over is_retriable 'where the semantics genuinely differ (brave matches Server(_), github matches Api { code: 500..=599, .. }, slack matches Network(_) \| Timeout(_))'. This enumeration is now false: commits 76a4d9b / 0bc7e57 / 7b65bfb (2026-08-06, issue #317, 'unify retry eligibility via ErrorCode::is_retryable') replaced the divergent match arms with an identical body in all three backends: `match e { XxxError::RateLimited { retry_after } => retry_after_within_cap(*retry_after), _ => e.classify().kind.is_retryable() }` (src/brave/client.rs:359-364, src/github.rs:394-399, src/slack/client.rs:602-607). The 'each backend keeps full control' framing is still structurally true (three private fns, no shared trait), but the parenthetical divergence claim that was the load-bearing reason to reject the Retryable trait option no longer matches the code. This postdates the last DR drift-fix pass (b8b8bc2, 2026-06-24), which reviewed and left this line unchanged when it was still accurate. The DR's Reassessment Triggers section also doesn't list 'is_retriable semantics converge across backends' as a trigger, so this newly-fired condition has no home in the document. Direction is dr-update, not code-fix: the current unified is_retriable (via ErrorCode::is_retryable) is a deliberate, reasonable consolidation, not drift away from a correct decision; the DR text is what's stale. The other Consequences bullets (delay formula centralized, no trait exposed, ~3-line extractor closure duplication) remain accurate at the single retry_with_rate_limit call site per backend (brave/client.rs:341, github.rs:155, slack/client.rs:186). | dr-update | M |

### DR 0007: Unify Brave client init via factory and Result-based https check

Status: accepted

Result: 2 findings (H 0 / M 1 / L 1).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0007-unify-brave-client-init-via-factory-and-result-based-https-check.md:87 | Reassessment Trigger 'A second HTTP backend is added (currently forbidden by ADR-0005)... The skip_https_check flag would need to generalize' was already moot when written: GitHubClient existed as a second HTTP backend since ADR-0004 (2026-05-13, 4 days before this DR), and the same-day commit 0d7e66c (2026-05-17, ticket RC-03, not RC-02) had already applied validate_https + a per-client skip_https_check flag to src/github.rs (see src/github.rs:71, :140, :141). Slack followed on 2026-05-30 (src/slack/client.rs:73, :210), and neither extension is credited in this DR (DR-0022 only documents Slack's token-prefix check, not its HTTPS-bypass field). The trigger's precondition has therefore already fired at 3 near-identical field/helper copies (brave/client.rs, slack/client.rs, github.rs) but was never evaluated against the Approach A rejection it was meant to gate (0007:60, 'for_wiremock is the same bypass renamed'). ADR-0005's parenthetical is the author's reason for expecting the precondition to stay false; it narrows nothing about what counts as an 'HTTP backend'. As written the trigger can never do its job of flagging future duplication because the duplication predates/coincides with the DR's own date. | dr-update | M |
| docs/decisions/0007-unify-brave-client-init-via-factory-and-result-based-https-check.md:49 | Three inline code pointers in the Decision Outcome/Pros-Cons prose are stale relative to current line numbers: 'validate_https&lt;E&gt;...(src/redacted.rs:37)' is now at src/redacted.rs:52; 'BraveClient supplies \|\| BraveError::InsecureBaseUrl (src/brave/client.rs:227)' is now at src/brave/client.rs:223; 'should_check_https() helper (src/brave/client.rs:210-219)' is now defined at src/brave/client.rs:206-215 (call site at :222-223). The referenced symbols and behavior are all still correct; only the anchors drifted from later, unrelated edits (doc-comment additions, module splits). | dr-update | L |

### DR 0008: Test seam architecture via `Arc<dyn Trait>` fields and `ScoutBuilder`

Status: accepted

Result: 5 findings (H 1 / M 0 / L 4).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0008-trait-injection-and-scoutbuilder-for-test-seam.md:40 | Decision Outcome states clock/rng/token_source "are forwarded into GitHubClient on first Scout::github() call (OnceCell lazy init)", framing GitHubClient as the single destination via one lazy mechanism. Current code fans clock/rng out to three destinations with different timing: GitHubClient lazily (src/tools.rs:222-231, matches DR), BraveClient eagerly inside ScoutBuilder::build() (src/tools/builder.rs:278-280, no OnceCell involved), and SlackClient lazily via its own OnceCell (src/tools.rs:246-254). with_clock/with_rng exist on three downstream client types (src/github.rs:116/121, src/brave/client.rs:195/200, src/slack/client.rs:159/164), confirming clock/rng are now general Scout-wide seam fields, not GitHub-specific plumbing as the sentence implies. token_source's half of the claim (gh auth token cost avoided for non-GitHub commands) still holds — only the clock/rng forwarding description is stale. | dr-update | H |
| docs/decisions/0008-trait-injection-and-scoutbuilder-for-test-seam.md:42 | Decision Outcome cites `with_clock` / `with_rng` / `with_token_source` at `src/tools/builder.rs:130-145`. That range currently holds the tail of ScoutBuilder::from_env's field construction (dns/egress/cancel/config init) and the for_test doc comment, not the three setters, which now sit at builder.rs:168 (with_clock), 174 (with_rng), and 180 (with_token_source) — shifted down by the same ADR-0009 DnsResolver insertion as the line-61 finding. | dr-update | L |
| docs/decisions/0008-trait-injection-and-scoutbuilder-for-test-seam.md:48 | Consequences example writes `StaticTokenSource(Some(Redacted::new("token")))`. `Redacted::new(s: &str) -> Option<Self>` (src/redacted.rs:13) already returns `Option<Redacted>`, and `StaticTokenSource` is a tuple struct over `pub Option<Redacted>` (src/token_source.rs:51), so `StaticTokenSource` expects `Redacted::new("token")` directly. Wrapping it in `Some(...)` produces `Option<Option<Redacted>>`, which would not type-check against the actual constructor. The example needs `StaticTokenSource(Redacted::new("token"))`. | dr-update | L |
| docs/decisions/0008-trait-injection-and-scoutbuilder-for-test-seam.md:61 | Confirmation cites the ScoutBuilder::build struct-literal grep result at src/tools/builder.rs:225. Current code has that literal (`Scout {`) at src/tools/builder.rs:281 — pushed down by the ADR-0009 DnsResolver field, with_dns setter, and related imports/doc comments added above it. The grep command itself is still valid and still returns exactly one hit; only the pinned line number is stale. | dr-update | L |
| src/tools/builder.rs:244 | The doc comment on with_github_endpoint says 'build() pre-inits the OnceCell with whatever clock / rng are set at that moment, so with_clock / with_rng have to come first' (mirrored at line 252 for with_slack_endpoint). But with_github_endpoint only assigns self.github_endpoint = Some(...); the actual GitHubClient construction reading self.clock/self.rng happens later, inside build() (lines 262-268), after the full with_* chain has already run. Since all with_* setters just mutate fields on a consumed-and-returned self before build() runs once at the end, the relative call order between with_clock/with_rng and with_github_endpoint/with_slack_endpoint cannot change the result - contradicting the comment's ordering claim, and also contradicting DR-0008's own Decision Outcome ('with_clock / with_rng / with_token_source / with_brave_endpoint / with_github_endpoint chain setters compose freely', DR-0008 line 42), which is the accurate description. | code-fix | L |

### DR 0009: Object-safe `DnsResolver` and `Arc<dyn DnsResolver>` injection via `ScoutBuilder`

Status: accepted

Result: 7 findings (H 1 / M 0 / L 6).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/tools/builder.rs:104 | build_default_clients(egress: &EgressMode) hardcodes Arc::new(SsrfResolver::new(TokioDnsResolver)) as the connect-time DNS resolver wired into fetch_http for EgressMode::Direct, and is called from both from_env and for_test before with_dns is ever applied. Scout.dns / ScoutBuilder::with_dns (DR-0009's seam) only reaches the pre-flight ssrf_check resolver via fetch_page/self.dns.clone(); it never reaches reqwest's ClientBuilder::dns_resolver, which always consults the real TokioDnsResolver regardless of an injected StaticDnsResolver/FailingDnsResolver. DR-0009's Decision Outcome claims injection 'plugs the double into every fetch path without touching tools.rs internals' (docs/decisions/0009-object-safe-dns-resolver-and-arc-injection.md:76) — true when DR-0009 was written (2026-05-19), but ADR-0012 (2026-05-30, docs/decisions/0012-connect-time-ip-guard-for-ssrf-dns-rebinding.md) added this second, non-injectable resolution point afterward. ADR-0012's own T-F072 confirms the split: connect-time resolver scripting goes through ScoutBuilder::with_fetch_http's separate .resolve()-based workaround (documented at src/tools/builder.rs's with_fetch_http doc comment), not through with_dns. The 'every fetch path' coverage claim in DR-0009 is stale post-ADR-0012 and should be corrected or cross-referenced. | dr-update | H |
| docs/decisions/0009-object-safe-dns-resolver-and-arc-injection.md:52 | Stale line-number citations in the Decision Outcome prose, left behind as the code grew after the DR was written. 'src/fetch/ssrf.rs:279-287' (cited at DR lines 52 and 126 for FailingDnsResolver/StaticDnsResolver) now points to unrelated code; FailingDnsResolver is actually at src/fetch/ssrf.rs:356-364 (the intervening SsrfResolver struct at ssrf.rs:298-329 was added later, presumably by ADR-0012). 'src/tools/builder.rs:130-151' (cited at DR line 54 for with_clock/with_rng/with_token_source) now points past those methods, which sit at builder.rs:168-184. 'src/tools/builder.rs:148' (cited at DR lines 54 and 124 for with_dns) is stale too; with_dns is now at builder.rs:186. None of these are content contradictions, just pointers invalidated by unrelated line-count drift. | dr-update | L |
| src/fetch/ssrf.rs:60 | DR-0009's Decision Outcome code block shows `pub(crate) type DnsLookupFuture<'a> = Pin<Box<dyn Future<...> + Send + 'a>>;` but the actual alias has no `pub(crate)` (module-private). Harmless in practice — `DnsLookupFuture` is only named inside ssrf.rs (all three implementors of `DnsResolver` live in this module), so no cross-module reference needs the wider visibility. The code's tighter privacy is the more correct contract; the DR's snippet is stale on this token. | dr-update | L |
| src/fetch/ssrf.rs:63 | DR-0009 lists as a named-and-accepted trade-off that 'the internal-helper signature split (Arc vs &dyn) is a judgment call that new contributors may not recognize. The rule is documented above and in the trait module's rustdoc' (docs/decisions/0009-object-safe-dns-resolver-and-arc-injection.md:79). The DnsResolver trait's rustdoc at src/fetch/ssrf.rs:63-66 only explains the Send+Sync bound ('so implementations can sit behind an Arc&lt;dyn DnsResolver&gt; shared across async tasks') and says nothing about when a call site should take Arc&lt;dyn DnsResolver&gt; (spawn/ownership sites) versus &dyn DnsResolver (pure-read sites). No other doc comment in fetch.rs, cdp.rs, download.rs, or engine.rs states this rule either. The mitigation DR-0009 named for its own stated risk does not exist in code. | code-fix | L |
| src/fetch/ssrf.rs:65 | DR-0009 Consequences (accepted 'Bad' item) states the Arc-vs-&dyn internal-helper split rule 'is documented above and in the trait module's rustdoc.' The trait's actual rustdoc (lines 63-64, plus the one-line `//!` module doc at line 1) only explains why `DnsResolver: Send + Sync` supports `Arc<dyn DnsResolver>`; it never states the call-site rule (Arc when ownership crosses a `tokio::spawn` move boundary, `&dyn` for pure reads) that the DR's helper-signature table lays out. A new contributor reading ssrf.rs rustdoc alone would not find the rule the DR promises is there. | code-fix | L |
| src/tools/builder_tests.rs:80 | DR-0009's Confirmation snippet illustrates injection with `Arc::new(StaticDnsResolver(vec!["10.0.0.1".parse().unwrap()]))`, but current tests (e.g. T-DNS001) construct the double via `StaticDnsResolver::single("10.0.0.1")`, a convenience constructor added after the DR was written. Semantically identical (same resolved IP set), purely a constructor-form staleness in the DR's illustrative code. | accept | L |
| src/tools/builder.rs:186 | DR-0009 states `ScoutBuilder` exposes `#[cfg(test)] pub(crate) fn with_dns(...)`, 'matching the #[cfg(test)] pub(crate) visibility of with_clock / with_rng / with_token_source (src/tools/builder.rs:130-151, with_dns at :148)'. In the current file, all four setters (`with_clock` :168, `with_rng` :174, `with_token_source` :180, `with_dns` :186) are `pub(super)`, not `pub(crate)`, and line :148 now points at `for_test`, not `with_dns`. The parity claim across the four setters still holds (all pub(super)); only the specific visibility token and the line pointer are stale. Code's `pub(super)` is the correct current contract (ScoutBuilder itself is `pub(super)`, so `pub(crate)` fns on it would not add real reachability) — the DR text should be corrected, not the code. | dr-update | L |

### DR 0010: Scout-local JSON envelope contract

Status: accepted

Result: 3 findings (H 1 / M 0 / L 2).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/github/types.rs:15 | DR-0010 Rule 4 requires every array field under the envelope's `data` key to serialize as `[]`, never `null`, and explicitly forbids `Option<Vec<T>>` for this reason. `RepoInfo::topics` is typed `Option<Vec<String>>` and derives `Serialize` with no `skip_serializing_if`/normalization. This same struct is embedded verbatim into the `repo-overview` JSON envelope at src/tools/repo.rs:212 (`"repository": repo_info`), so when GitHub returns `topics: null` (a real, reachable case — pinned by the mock fixtures at src/tools/repo.rs:366, src/tools/repo.rs:463, and src/tools/repo_io_tests.rs:68, all using `"topics": null`), the public `--json` output emits `data.repository.topics: null` instead of `[]`. No test asserts what `data.repository.topics` actually renders to in this null-topics scenario, so the drift is untested as well as unfixed. This is exactly the failure mode Rule 4 (and its Consequences section, which says a bare `Option<Vec<T>>` is not an acceptable workaround) was written to prevent: an AI-agent parser calling `.topics.length` or `Array.isArray(data.repository.topics)` on `repo-overview --json` output breaks. | code-fix | H |
| src/envelope.rs:1 | DR-0010's Consequences (Bad) section states a follow-up PR is still needed to migrate code-side `per ADR-0065` references (named: src/envelope.rs, src/lib.rs, src/tools/errors.rs, tests/cli_integration.rs) to `per ADR-0010`. `grep -rn "ADR-0065" src/ tests/` returns zero hits — src/envelope.rs:1's doc comment already reads "per ADR-0010 (scout-local JSON envelope contract) and ADR-0003", with no ADR-0065 reference anywhere in the four named files or elsewhere in src/tests. The migration this bullet describes as outstanding has already happened; the DR's Consequences section was not updated afterward to record that the debt was paid off. | dr-update | L |
| src/envelope.rs:208 | DR-0010's Confirmation section pins implementation line ranges that have drifted from the current file layout, so a reader following the DR to verify Rule 1/2/3 lands on the wrong code. DR says `is_retryable` is at `src/envelope.rs:201-206`; it is actually at 208-210 (line 201 now falls inside `exit_code`'s `Self::Timeout => 124` arm). DR says `SuccessEnvelope` is at `:211-218`; it is actually at 213-222. DR says `ErrorPayload` is at `:228-236`; it is actually at 239-249. The Addendum cites `fetch_sources` at `src/search/engine.rs:55-122`; it is actually at 88-134 (`sources.iter().take(depth)`, the invariant-relevant loop, sits at line 97, not 55). The Addendum cites `format_sources` at `:169-177`; it is actually at 233+. The semantic content each range describes still matches the code (verified: is_retryable == matches!(TempFailure\|Timeout); SuccessEnvelope/ErrorPayload field-omit annotations match Rules 2/3 exactly; fetch_sources's take(depth) still produces the len(fetched_pages)+len(failed_urls) &lt;= depth &lt;= len(sources) invariant), so this is a stale-pointer drift, not a behavioral one — the DR's line anchors have not been updated as the file grew (doc comments, new fields) around them. | dr-update | L |

### DR 0011: Scout-local Classification Priority Policy

Status: accepted

Result: 5 findings (H 2 / M 1 / L 2).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/fetch.rs:132 | The same semantic condition (upstream response exceeds scout's byte cap) maps to different ErrorCodes across backends: FetchError::TooLarge -&gt; DataError/65 (src/fetch.rs:132), but GitHubError::ResponseTooLarge -&gt; Internal/70 (src/github/errors.rs:110, explicitly pinned 'peer to Decode' by T-GHC010) and BraveError::ResponseTooLarge -&gt; Internal/70 (src/brave/client.rs:108). Both are non-retryable so `retryable` doesn't diverge, but the public `error.code`/exit-code contract does (65 vs 70) for what a caller would read as the same class of failure. The DR table never names a 'response too large' condition under any priority row, so each backend derived its own analogy (fetch: caller-actionable URL choice -&gt; DataError priority 2; GitHub/Brave: bounded-API contract violation, analogous to the DR's own 'schema mismatch' priority-5 example -&gt; Internal). The analogies are individually defensible but produce inconsistent exit codes for the same failure family, and the DR is silent on which analogy governs. | dr-update | H |
| src/tools/query.rs:20 | DR-0011 priority 3 names 'search 0 件' as a NotFound (exit 66) condition, but `search()` returns a success envelope with empty `sources` and exit 0 for zero Brave results (pinned by T-TS027 in src/tools/query_tests.rs:105, and by T-TS0xx research_json_zero_results_returns_empty_arrays for research). No production path constructs ErrorCode::NotFound for a zero-result search; `ScoutError::not_found()` (src/tools/errors.rs:80) is `#[cfg(test)]`-only and unused in this path. This is a direct contradiction of the DR's own explicit example, not an edge case the table left open. | dr-update | H |
| src/classify.rs:65 | DR priority 2 reads 'API 4xx (other than 401/403/404)' -&gt; DataError, and priority 4 separately lists 'rate limit' -&gt; TempFailure without naming a status code. `from_http_status` sends 408 and 429 to `transient_retry()` (priority 4) ahead of the generic 400..=499 DataError arm, which is sensible (429/408 are textbook retryable), but a literal top-down read of the DR's own exclusion list (which names only 401/403/404) would place 408/429 under priority 2 DataError instead. The table is self-inconsistent on this point and the code silently resolved it; no test (T-ER023/T-ER024 only cover 4xx/5xx generically) pins the 408/429 placement against the table text. | dr-update | M |
| docs/decisions/0011-scout-local-classification-priority-policy.md:80 | Confirmation clause names only the 4 per-backend classify() files (src/fetch.rs, src/github/errors.rs, src/slack.rs, src/brave/client.rs) as where priority order is verifiable via `// Priority N:` inline comments. Since the Classification type moved out (per the doc's own "参照" list at line 139: `src/classify.rs` is now `Classification`'s home), the shared HTTP-status and reqwest-error priority decision now lives in `from_http_status` (src/classify.rs:63-73) and `from_reqwest` (src/classify.rs:87-98), which carry their own `// Priority 4: TIMEOUT` (:88), `// Priority 4: TEMP_FAILURE` (:92), and `// Retreat:` (:96) comments. All four listed backends delegate their 4xx/5xx/timeout classification to these two functions (e.g. src/fetch.rs:146, src/github/errors.rs:112). A reader following the Confirmation procedure literally as written never opens the file where priority 1/2/4 for HTTP status codes and transport errors is actually decided. | dr-update | L |
| src/github.rs:209 | DR table row 1 reads "auth misconfig (401/403)" as an unconditional priority-1 UsageError condition, but GitHub 403 responses do not classify that way by default: per ADR-0004 Rule 1 (cited inline at src/github.rs:210-214, and again at src/github/errors.rs:66-68/74), a 403 maps to `GitHubError::Forbidden` (priority 1) only when `x-ratelimit-remaining` is present and &gt;0; otherwise it maps to `GitHubError::RateLimited` (priority 4, TempFailure), tested by T-GH004 (src/github/http_tests.rs:132) and T-GH010 (src/github/http_tests.rs:152). The deviation is deliberate, cited to a named ADR, and test-pinned, so this is accept rather than a live gap, but the DR-0011 table's own row 1 text does not mention the carve-out or reference ADR-0004, so a reader relying on the table alone would expect all 403s to be priority 1. | accept | L |

### DR 0012: Connect-time IP Guard for SSRF DNS Rebinding, with CDP Path Asymmetry

Status: accepted

Result: 3 findings (H 0 / M 0 / L 3).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0012-connect-time-ip-guard-for-ssrf-dns-rebinding.md:31 | Decision Outcome text pins the SsrfResolver injection site as src/tools/builder.rs:73, but the actual dns_resolver() call site is src/tools/builder.rs:104 (build_default_clients grew a proxy/direct match arm between the two, shifting the line). Code is correct and matches the DR's intent (SsrfResolver injected via ClientBuilder::dns_resolver for fetch_http in Direct mode); only the pinned line number in the DR prose is stale. | dr-update | L |
| docs/decisions/0012-connect-time-ip-guard-for-ssrf-dns-rebinding.md:37 | Decision Outcome body states the CDP/chromium rebind hole 'remains, out of scope, tracked in a separate issue' (#201). This is now stale as a standalone claim: src/fetch/cdp/proxy.rs (loopback SOCKS5 proxy, fail-closed is_private_ip validation on every CONNECT target) and src/fetch/cdp/launch.rs (--proxy-server/--proxy-bypass-list=&lt;-loopback&gt;/--disable-quic launch flags) close that hole. This is not silent drift: the DR's own 'Addendum: CDP 経路の carve-out 解消 (issue #201)' at line 104 and the '参照' cross-reference at line 101 already record the resolution and supersede the body paragraph. Documented, no action needed beyond noting the body paragraph is superseded by the Addendum that follows it in the same file. | accept | L |
| src/fetch/fetch_page_tests.rs:105 | T-F072's non-tautology argument (DR Confirmation: 'guard壊れていてもis_err()は真になるがwarnは出ない') assumes the connect-time guard is the only source of the 'blocked connect to private IP' message, but ssrf.rs's first_blocked_ip() emits that identical message text for all three stages (preflight/connect/proxy), differing only in a structured `stage` field that tracing_test's logs_contain() does not match on. Verified not to be live drift: this specific test's preflight resolver always returns a public IP (93.184.216.34), so first_blocked_ip("preflight", ...) never fires and the message can only originate from the connect-stage SsrfResolver in this test, preserving the DR's stated detection property today. Flagged as accept because the current test construction avoids the ambiguity, but the shared message string is a latent fragility versus the DR's stated non-tautology rationale: a future test reusing this pattern with a private preflight IP would defeat the discriminant. | accept | L |

### DR 0013: Charset Detection and Decode Policy

Status: accepted

Result: 6 findings (H 0 / M 1 / L 5).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0013-charset-detection-and-decode-policy.md:32 | Decision Outcome states '両経路とも宣言エンコーディングを起点に復号し、失敗時のみ信頼ゲート付き chardetng 検出へ落とす' (Option A restates this at line 53), implying both paths retry via chardetng when the declared encoding fails to decode. That is true for fetch (decode_body -&gt; detect_decode on had_errors, src/fetch/download.rs:149-186) but false for the GitHub path: decode_explicit (src/github/encoding.rs:67-86) returns NonUtf8 immediately when the --encoding hint fails, and decode_bom (src/github/encoding.rs:99-117, hardened by T-GE015 in the same commit that touched this DR) returns Err immediately when a present BOM fails to decode -- neither falls through to decode_detect's chardetng gate. On GitHub, chardetng only runs in decode_bytes's third branch, when there is no declaration at all (no hint, no BOM), not as failure recovery from a declared-but-wrong encoding. The rest of the DR is internally consistent with this actual (asymmetric) behavior: Option C's rejection reason names the multi-byte-recovery benefit as fetch's property alone ('fetch のリカバリを潰すため'), decode_bom's own doc comment at src/github/encoding.rs:88-98 argues from ADR-0013 that a failed declared BOM must hard-error, and Consequences bullet 1 only says fetch recovers automatically. Only the two summary sentences (line 32, line 53) overclaim symmetry. Code is correct and tested (T-GE015/T-GE016); the DR's own summary prose is stale relative to its own detail sections. | dr-update | M |
| docs/decisions/0013-charset-detection-and-decode-policy.md:43 | Consequences bullet (and the matching Reference-section bullet at line 115) says the GitHub-path BR-001/002/003 / FR-007/008 comment codes are resolved by this ADR but that repointing the source comments is tracked as a separate task ('別タスクで追跡する'), implying the comments still cite the dangling codes. They no longer do: grep of src/github/ finds zero BR-00x/FR-00x references. decode_bytes, decode_bom, and decode_detect in src/github/encoding.rs already cite 'ADR-0013' directly (lines 51, 93, 134, 158). That repoint landed in commit 445ff57 (#263), the same day the ADR was created, before the ADR's own text was ever revisited. The ADR still describes finished work as an open follow-up. | dr-update | L |
| docs/decisions/0013-charset-detection-and-decode-policy.md:79 | More Information line-range citations point past the current function bodies after later edits shifted the code: 'decode_body (src/fetch/download.rs:137-174)' (line 79) actually spans 149-186 now, and 'decode_bytes (src/github/encoding.rs:60-161)' (line 87) actually spans the decode_bytes/decode_explicit/decode_bom/decode_detect family at 57-173. Same drift class as the Reference section's src/fetch/download.rs:103-193 and src/github/encoding.rs:52-161 pointers (lines 112-113). A reader following any of these line anchors lands a dozen-plus lines off the intended function. | dr-update | L |
| src/github/encoding.rs:51 | decode_bytes's doc comment lists detection priority as 5 numbered steps ('1. hint 2. BOM 3. chardetng 4. AssumedUtf8 5. NonUtf8 error') phrased as a sequential fallback chain. Step 2 (BOM) actually terminates on failure (returns Err without proceeding to step 3), which decode_bom's own adjacent doc comment (lines 88-98) explains correctly but this summary list does not reflect. Minor internal inconsistency between two doc comments in the same file, same root cause as the helpers.rs:102 finding. | code-fix | L |
| src/github/encoding.rs:158 | Comment attributes the AssumedUtf8 backstop branch to 'the documented detection priority (ADR-0013)', but the DR's Decision Outcome text never enumerates an AssumedUtf8 tier (it only speaks of label/hint-first, detection-gated fallback, and a path-specific last resort). The DR-derived priority list actually enumerating AssumedUtf8 lives in this file's own decode_bytes docstring (line 51-56), not in the DR itself -- a loosely traceable, low-stakes reference rather than a verified DR citation. | accept | L |
| src/github/helpers.rs:102 | decode_content's doc comment says 'When None, chardetng auto-detects the encoding (BOM -&gt; chardetng -&gt; UTF-8 fallback)', presenting a sequential fallback chain. Per decode_bytes (src/github/encoding.rs:57-65) and decode_bom (src/github/encoding.rs:99-117), a present-but-undecodable BOM short-circuits to a hard NonUtf8 error and never reaches chardetng or the UTF-8 fallback -- this was the exact regression T-GE015 was added to close. The comment predates or was not updated alongside that fix and now misdescribes the public-facing contract of a pub(crate) function with 3 call sites (src/tools/repo.rs:127,130,322). | code-fix | L |

### DR 0014: Output-Injection Defense for AI-Agent Consumers

Status: accepted

Result: 8 findings (H 3 / M 2 / L 3).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0014-output-injection-defense-for-agent-consumers.md:34 | Decision Outcome's opening sentence says neutralization is concentrated in pub(crate) functions in `src/markdown.rs` and `src/fetch/converter.rs`. Since the 2026-08-03 yaml-helper-module-move refactor, the YAML/marker neutralization primitives (`neutralize_yaml_markers`, `neutralize_yaml_markers_outside_fences`, `write_yaml_str`, `escape_yaml`) live in `src/yaml.rs`, not `src/fetch/converter.rs`. Verified: `src/fetch/converter.rs` declares no pub(crate) neutralization function itself (only private `fn`s; `format_with_frontmatter` at line 989 is private and just calls `crate::yaml::neutralize_yaml_markers_outside_fences`, imported at line 13). The DR's own More Information table (line 101-103) and References section (line 125-126) correctly cite `src/yaml.rs`, so only this top-line summary sentence was missed when the module was split out (the migration plan `.claude/workspace/planning/2026-08-03-yaml-helper-module-move.plan.md` U-003 updated the table/references but not this sentence). This is the DR's single highest-authority claim ('Chosen option') and is read first by anyone auditing the security boundary shared by fetch/search/Slack/GitHub. | dr-update | H |
| src/markdown.rs:225 | The DR's 中和点 table row for `shift_headings` (docs/decisions/0014-...md:99) states its guard as only '(code fence 内・非 ATX は skip)'. The function also rewrites setext headings to ATX (src/markdown.rs:258-266) and skips a leading frontmatter block via `frontmatter_len` (src/markdown.rs:113-121) specifically because an unshifted closing `---` was being read as a setext underline and consumed, dropping the delimiter that closes the frontmatter block (regression pinned by T-MD026, src/markdown.rs:439-453). That guard sits directly on the YAML-marker boundary this DR is about and is undocumented. `shift_headings` has 3 non-test callers (src/tools.rs:317, src/search/engine.rs:199, src/github/format.rs:176). | dr-update | H |
| src/yaml.rs:112 | DR-0014's Decision Outcome (docs/decisions/0014-...md:36) and Consequences (line 48) describe fetch's fence fail-closed switch as the whole story, and the 中和点 table (line 93-106) and Confirmation section (line 59) enumerate only escape_yaml/neutralize_yaml_markers/neutralize_yaml_markers_outside_fences/format_readme_section/format_tree/format_file_content. Neither section mentions `reneutralize_dangling_fence` (src/yaml.rs:82) or `truncate_and_reneutralize` (src/yaml.rs:112), which close a second, distinct hole: a byte cap applied after neutralize_yaml_markers_outside_fences can cut inside a still-open fence and expose a marker that was correctly left verbatim before the cut. `truncate_and_reneutralize` is pub(crate) and has 2 non-test call sites (src/tools.rs:319, src/search/engine.rs:200), so it is load-bearing production code the DR's own audit table omits. | dr-update | H |
| docs/decisions/0014-output-injection-defense-for-agent-consumers.md:100 | The DR attributes the shared, stateful fence open/continue/close tracking (used by both `shift_headings` and `neutralize_yaml_markers_outside_fences`) to `fence_marker` (src/markdown.rs), in 4 places: Decision Outcome (line 36: '`fence_marker` (src/markdown.rs) でフェンスの開始・継続・終了を追跡する'), the More Information table (line 100), Confirmation (line 59, 'フェンス判定 (`fence_marker`)'), and References (line 124). In the current code, `fence_marker` (src/markdown.rs:199) is a private, stateless, single-line classifier with no `pub(crate)` and no continuation logic; the actual shared `pub(crate)` function both `shift_headings` (markdown.rs:251) and `neutralize_yaml_markers_outside_fences` (yaml.rs:8,59, imported as `crate::markdown::track_fence`) call is `track_fence` (markdown.rs:185), which owns the open/close-length comparison (CommonMark run-length rule) and calls `fence_marker` only as an internal helper. The code's own test comment (T-MD035, markdown.rs:655-657) already states this distinction explicitly ('`track_fence` owns the trim'), so this is a documentation naming error, not a code defect. An auditor following the DR's own instruction ('関数名は `ugrep -F 'fn <name>'` で一意に引ける', line 91) to check `fence_marker` would land on the wrong function and could miss changes to the actual shared/stateful contract in `track_fence`. | dr-update | M |
| src/yaml.rs:137 | The DR's table row for `escape_yaml` (docs/decisions/0014-...md:101) lists only the escape set (`\` `"` `\n\r\t`, `\0` removed). It omits `truncate_field`/`MAX_FIELD_BYTES` (src/yaml.rs:137-156), which truncates a frontmatter value to a byte cap before escaping so a value made entirely of backslashes cannot leave a lone trailing `\` that escapes the closing quote and never lets the YAML scalar close (T-FC102, src/yaml.rs:358-393). `truncate_field` has one caller, `write_yaml_str` (src/yaml.rs:167). | dr-update | M |
| docs/decisions/0014-output-injection-defense-for-agent-consumers.md:67 | Option A's pros/cons list 'a single audit point (5 functions)' as a Good. The More Information table this DR itself maintains (lines 93-106) now lists 12 pub(crate) neutralization-related functions across 3 files (`src/markdown.rs`: md_link, escape_md_inline, escape_md_link, sanitize_heading, shift_headings, fence_marker/track_fence; `src/yaml.rs`: escape_yaml, neutralize_yaml_markers, neutralize_yaml_markers_outside_fences; `src/github/format.rs`: format_readme_section, format_tree, format_file_content), grown from the original 5 (md_link, escape_md_inline, escape_md_link, sanitize_heading, escape_yaml) as fence-tracking and the GitHub README/tree/file-content paths were added later. The '5 関数' count is stale relative to the DR's own current table; low-priority since it is a summary framing number in a historical Pros/Cons section, not a functional or safety claim. | dr-update | L |
| src/markdown.rs:185 | The DR's Decision Outcome (docs/decisions/0014-...md:36) and table row (line 100) say `neutralize_yaml_markers_outside_fences` tracks fences via `fence_marker`, and that `fence_marker` is shared between `shift_headings` and `neutralize_yaml_markers_outside_fences`. In code, both callers actually go through `track_fence` (src/markdown.rs:185, pub(crate)), which owns the trim and open/close state transition; `fence_marker` (src/markdown.rs:199) is a private, lower-level classifier called only from inside `track_fence` and has no direct callers in src/yaml.rs. The DR names the wrong function as the shared audit point, which matters because the DR states its table is meant to be grepped by function name for audit (docs/decisions/0014-...md:91). | dr-update | L |
| src/markdown.rs:577 | DR Confirmation (docs/decisions/0014-...md:59) cites markdown-layer coverage as `[T-MD001..T-MD035]`. T-MD036 (src/markdown.rs:555) and T-MD037 (src/markdown.rs:577) exist and pin truncation not leaving a partial line or a lone `---` (thematic-break/marker forgery) after a byte-cap cut in `truncate_with_note` — directly relevant to this DR's marker-forgery concern — but sit outside the cited range. | dr-update | L |

### DR 0015: Redacted Mandatory Secret Carrier

Status: Accepted

Result: 2 findings (H 0 / M 2 / L 0).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/brave/client.rs:146 | DR-0015's core rationale for choosing Option A over Option B is that a newtype makes redaction automatic and not dependent on per-site human discipline ("Good, because 構造体の derive Debug が [REDACTED] を出し、偶発的な log 漏洩を塞ぐ"). BraveClient's Debug impl, however, is hand-written and does not call Redacted's Debug at all -- it hardcodes the literal `&"<redacted>"` string for the `api_key` field instead of `#[derive(Debug)]`ing the struct and letting `Redacted`'s own masking Debug impl (redacted.rs:41-45) do the work. This reintroduces exactly the per-site manual-discipline dependency the DR rejected Option B for: if a future secret field is added to BraveClient, the type system will not force it into this manual field list the way `#[derive(Debug)]` would automatically pick up any `Redacted`-typed field. There is also no test asserting BraveClient's own `{:?}` output masks the key (T-RD001 only covers `Redacted` in isolation; T-RD/T-BC test files have no BraveClient-Debug assertion), so a regression here (e.g. someone switching to `#[derive(Debug)]` without checking the manual impl is gone, or adding a new secret field and forgetting to mask it) would not be caught by the DR's own Confirmation-listed tests. | code-fix | M |
| src/redacted.rs:29 | DR-0015's Decision Outcome states construction happens '構築は Redacted::new(&str) -&gt; Option&lt;Self&gt; のみで' (only via `new`) and that the env-var boundaries at token_source/slack client/brave client '必ず Redacted::new を通す', with the Bad-consequence parenthetical reaffirming '現状は全境界が Redacted::new 経由'. The code has since grown a second pub(crate) constructor, `Redacted::from_env_var(name, get_var, err) -> Result<Self, E>` (src/redacted.rs:29-38, added per issue #311 per the comment at src/slack/client/constructor_tests.rs:27-29), which src/brave/client.rs:169 and src/slack/client.rs:139 call directly instead of `Redacted::new`. Only src/token_source.rs:70,102 (the GitHub path) still call `Redacted::new` directly as the DR literally describes. Deleting `from_env_var` and inlining it at both call sites would reproduce the same `get_var(name).ok().and_then(\|raw\| Redacted::new(&raw)).ok_or_else(err)` chain twice, so the function itself earns its keep — the drift is that the DR's prose no longer matches which function actually sits at 2 of 3 boundaries. The safety invariant is preserved (from_env_var delegates to `Self::new` internally, and has its own dedicated tests T-RD006/T-RD007 not cited in the DR's Confirmation section), so this is a documentation-accuracy gap rather than a behavior or security regression. | dr-update | M |

### DR 0016: GitHub Formatter Output Schema and README Byte Cap

Status: accepted

Result: 7 findings (H 0 / M 1 / L 6).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0016-github-formatter-output-schema.md:39 | DR-0016's Consequences (Bad-because, lines 39-41) list only 'content past the cap is lost / cap is fixed / truncation is silent' but omit that the cut can land inside an open code fence in the README's kept portion. Since commit 1f6c335 (#430, 2026-08-16) added neutralize_yaml_markers_outside_fences between shift_headings and the truncation marker (src/github/format.rs:166-177), a truncation cut that leaves a fence open now causes the fail-closed branch to rewrite '---'/'...' lines to '***' even in the retained (pre-cut) portion of the README, not only drop the tail. T-GF045 (src/github/format/overview_tests.rs:386-402) exercises exactly this behavior. DR-0014 was updated in the same commit to note GitHub README shares fetch's fence-tracking path, but neither DR records this cap-truncation/neutralization interaction as a consequence of the byte cap. | dr-update | M |
| docs/decisions/0016-github-formatter-output-schema.md:29 | Decision Outcome enumerates the schema as title, metadata table, README, Issues, PRs, Releases (also DR:73's table). Neither mentions the optional description paragraph that `format_overview` emits between the title and the metadata table (src/github/format.rs:123-125, escaped via escape_md_inline, present whenever repo.description is Some). An agent (or a future maintainer) relying on the DR's documented schema to parse or reason about output shape would not expect this paragraph. | dr-update | L |
| docs/decisions/0016-github-formatter-output-schema.md:73 | DR's section-order table cites 'src/github/format.rs:106-267' as the primary source for the six format_* functions. In current code `format_overview` starts at line 114 and `format_releases_section` ends at line 272 (also repeated at DR line 115). The range has drifted by ~8 lines on both ends. | dr-update | L |
| docs/decisions/0016-github-formatter-output-schema.md:93 | DR cites 'src/github/format.rs:153-174' for the truncation logic block. `format_readme_section` now spans lines 160-182, and the floor_char_boundary/newline-snap/shift_headings sequence the DR's code excerpt shows is at lines 166-176. The cited range no longer brackets the described code. | dr-update | L |
| docs/decisions/0016-github-formatter-output-schema.md:103 | DR states 'truncate_with_note を再利用しないのは、切り詰めとマーカー付与の間に shift_headings を挟む必要があるため' (one reason). The code comment at src/github/format.rs:163-165 gives two reasons: inserting shift_headings, and that neutralize_yaml_markers_outside_fences must also run between the cut and the note so the fail-closed branch can see a fence the cut left open. The DR's rationale is incomplete relative to the code's own stated invariant; a reader following only the DR could 'simplify' back to truncate_with_note and silently drop the fail-closed neutralization ordering. | dr-update | L |
| docs/decisions/0016-github-formatter-output-schema.md:115 | DR cites `src/github/format.rs:6` for `MAX_README_BYTES = 24_000`. The constant now sits at format.rs:9 (three unrelated `use` lines were added above it). Mechanical, but a reader following the citation lands on `use crate::yaml::neutralize_yaml_markers_outside_fences;` instead of the constant. | dr-update | L |
| src/github/format.rs:161 | DR states '空の section は省く' (empty sections are omitted) as a blanket rule, and the code implements it correctly for issues/pulls/releases via `.is_empty()` checks. The README section instead omits only on `Option::None` (`let Some(content) = readme else { return };`); a `Some("")` reaches format_overview and still emits a bare '## README' header with no body. Currently guarded: the sole call site (src/tools/repo.rs:308,310) filters `.filter(\|c\| !c.is_empty())` before decoding, so `Some("")` never actually reaches format_readme_section today. Flagging because the function-level contract diverges from the DR's stated invariant and would silently regress if a second caller skipped that filter. | code-fix | L |

### DR 0017: Signal Exit Codes and Graceful Drain

Status: accepted

Result: 6 findings (H 1 / M 2 / L 3).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/lib.rs:197 | Under `--json`, the `Outcome::Interrupted` arm in `run()` does `eprintln!("error: interrupted ({sig})")` unconditionally, ignoring `json_mode`. Every other error-emitting arm in the same match (`emit_error` at line 175/196, `write_failure_line` at line 191, `handle_parse_error`) branches on `json_mode` and emits a JSON envelope. T-W004's doc comment (line 432-435) states the contract explicitly: "The flag promises every error on stderr is a JSON envelope, so a caller parsing stderr must not receive a bare line for this path." DR-0017's whole premise is that shell/machine callers key off SIGINT/SIGTERM exit codes; under `--json` those same callers get a bare, non-JSON stderr line instead of `{"error":{...}}` at exit 130/143 exactly on the interrupt path the DR is about. `ErrorCode` (envelope.rs:176-185) also has no `Interrupted` variant, so there is no envelope shape to reuse yet — this needs new code, not just a branch. | code-fix | H |
| docs/decisions/0017-signal-exit-codes-and-graceful-drain.md:32 | DR derives SHUTDOWN_DRAIN_TIMEOUT=7s as 'browser.close() の内部 timeout (5s) + chromium subprocess cleanup の余白', and the More Information drain section (line 89) names only 'browser.close() → subprocess reap' without quantifying the reap step. Tracing the actual code: timeout(5s, browser.close()) (src/fetch/cdp.rs:218) falls through unconditionally to reap_pgroup (src/fetch/cdp/launch.rs:225-256), whose own ceiling is a 50ms SIGTERM grace (PGROUP_SIGTERM_GRACE, launch.rs:126) plus timeout(2s, child.wait()) (launch.rs:248) — 5s+0.05s+2s = 7.05s against the 7.00s outer drain. launch.rs:244-247's comment ('so SHUTDOWN_DRAIN_TIMEOUT calibration stays honest', issue #152) shows the 2s reap budget was deliberately sized against the drain timeout, but the DR's own derivation never names or reconciles it, so the DR's arithmetic rationale is stale relative to the working code. | dr-update | M |
| src/lib.rs:40 | `SHUTDOWN_DRAIN_TIMEOUT = 7s` is derived in the doc comment as "CDP browser.close() (5s) plus chromium's own subprocess cleanup margin." The actual worst-case chain that must complete inside this budget is: timeout(5s, browser.close()) at src/fetch/cdp.rs:218, then (if the pgroup didn't already exit) PGROUP_SIGTERM_GRACE = 50ms at src/fetch/cdp/launch.rs:126, then timeout(2s, child.wait()) at src/fetch/cdp/launch.rs:248 — 5s + 50ms + 2s = 7.05s, which exceeds the 7s drain budget by 50ms. On that worst-case path, drive()'s outer timeout (lib.rs:147) fires while still inside reap_pgroup and drops the `child.wait()` future — the exact 'zombie left for the OS reaper' case the comment at launch.rs:244-247 names as the thing SHUTDOWN_DRAIN_TIMEOUT calibration must stay honest about. None of the three constants (7s, 5s, 50ms+2s) are coupled in code; the 'sufficient' claim lives only in prose and is arithmetically not quite true on the composed worst case. | code-fix | M |
| docs/decisions/0017-signal-exit-codes-and-graceful-drain.md:48 | Confirmation describes T-H000 as asserting --help contains the literal phrase "Interrupted by SIGINT/SIGTERM". The actual test (src/lib.rs:527-534) asserts the bare substrings "SIGINT" and "SIGTERM" separately, not the joined phrase. The help text itself does contain the full phrase (src/lib.rs:76-77), so the failure mode the DR describes is still caught; the DR's test description is just more specific than what the assertion checks. | accept | L |
| src/fetch/cdp.rs:218 | DR-0017 describes the 5s figure as CDP `browser.close()`'s "internal timeout." It is not internal to CDP/chromiumoxide — it's scout's own `timeout(Duration::from_secs(5), browser.close())` wrapper. This mischaracterization is what makes the 7s derivation (see the SHUTDOWN_DRAIN_TIMEOUT finding) hard to audit: the DR treats 5s as an external, fixed constant scout must accommodate, when it's actually scout's own knob that could be tuned jointly with SHUTDOWN_DRAIN_TIMEOUT. | dr-update | L |
| src/lib.rs:479 | DR-0017's Confirmation section states T-H000 asserts `--help` contains the phrase "Interrupted by SIGINT/SIGTERM". The actual test (lib.rs:499-534) only asserts the substrings "130", "143", "SIGINT", "SIGTERM" independently anywhere in the help text, not the joined phrase. The literal phrases do exist in the help text (lines 76-77), so production behavior matches intent — this is a description-accuracy gap in the DR's Confirmation section, not a behavior gap. | dr-update | L |

### DR 0018: GitHub Token Resolution Precedence and Leak Containment

Status: accepted

Result: 3 findings (H 1 / M 0 / L 2).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0018-github-token-resolution-precedence.md:48 | Confirmation closes with '(この注入を入れるまで) ... どの分岐を走るかが、テストを回すマシンに gh が入っているかに依存していた' stated as resolved by the spawn_gh injection. That injection only reaches src/token_source.rs's own unit tests (T-TOK001-006). ScoutBuilder::for_test() (src/tools/builder.rs:157) still defaults token_source to the production GhCliSource rather than StaticTokenSource, so Scout::github() lazy-init tests built on for_test() — e.g. [T-TS016] github_lazy_init_from_empty_cell (src/tools/repo_lazy_tests.rs:429) via scout_lazy (src/tools/test_helpers.rs:32) — still spawn the real `gh auth token` subprocess (or read real env) depending on the test machine, the exact non-determinism the DR's closing sentence claims is behind it. for_test() has 23 call sites across test_helpers.rs and builder_tests.rs, so the gap is not confined to one test. | dr-update | H |
| docs/decisions/0018-github-token-resolution-precedence.md:30 | DR states the subprocess stderr 'は読まない' (is not read). In the implementation, tokio::process::Command::output() (src/token_source.rs:43-46) captures both stdout and stderr into the returned Output before resolve_from_env_or_gh ever runs; the code simply never accesses output.stderr afterward. The leak-containment outcome holds (nothing reaches the log, pinned by T-TOK004), but the mechanism the DR describes is stronger than what the code does: the secret does transit process memory via the captured pipe, it just is not forwarded to logs. | dr-update | L |
| docs/decisions/0018-github-token-resolution-precedence.md:86 | More Information line citations are stale relative to current src/token_source.rs: TOKEN_RESOLVE_TIMEOUT cited at ':15' is now at line 17; 'GhCliSource'/'resolve_from_env_or_gh' cited as ':26-93' now spans lines 29-103; the SEC non-zero-exit/stderr-withhold block cited as ':80-90' is now at lines 89-100 (same drift repeats in the ':26-93' reference on line 90). | dr-update | L |

### DR 0019: Environment-Variable Validation and Timeout Hierarchy

Status: accepted

Result: 3 findings (H 1 / M 1 / L 1).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/tools/config.rs:168 | DR-0019's Decision Outcome states the outer/inner timeout hierarchy is preserved even under `SCOUT_*` override ('SCOUT_* override 時もこの大小を崩さない値域 (≤600s) に収める'), but `parse_timeout` validates `github_timeout` against the same shared `TIMEOUT_MIN_SECS..=TIMEOUT_MAX_SECS` (1..=600) range used for fetch/research/slack, with no floor tied to the inner per-request timeouts. Setting `SCOUT_GITHUB_TIMEOUT_SECS=5` (config.rs:83-94 -&gt; :168) is accepted, but 5s sits below `HTTP_TIMEOUT` (30s, src/tools/builder.rs:27), so the outer timeout wrapping every GitHub command (src/tools.rs:266, called from :287/291/295) fires before a single request can complete -- reproducing exactly the outer&lt;inner failure mode Option A was chosen to prevent. The only invariant test, T-CFG021 (config.rs:389-410), asserts the hierarchy for `RuntimeConfig::default()` only, so it cannot see this override-time break, and the DR's own 'Bad' consequence bullet scopes the risk to 'future changes to internal constants,' not to an operator-supplied override value -- leaving this specific gap unacknowledged anywhere in the DR text. | code-fix | H |
| src/lib.rs:85 | DR-0019 claims the boundary constants/defaults in `src/tools/config.rs` and the `--help` (after_help) range text 'point to the same source of truth' ('help・テスト・実装が乖離しない'), but `after_help` in src/lib.rs:65-93 is a plain string literal that hand-duplicates the ranges (1-600, 0-10) and defaults (95/45/60/180, 2) with no code-level reference back to `TIMEOUT_MIN_SECS`/`TIMEOUT_MAX_SECS`/`RETRIES_CAP` (config.rs:33-44) or `DEFAULT_MAX_RETRIES` (src/retry.rs:24). Nothing enforces the two stay in sync: a future change to any of those constants would silently desync the printed help from actual validated ranges, which is the exact class of divergence the DR's consequence claims is structurally prevented. The repo already has the matching precedent for this kind of pin -- `tests/cli_integration.rs:28-34` (T-CFG001-adjacent test) asserts each exit code literally appears in `--help` output, sourced from the ADR-0002 contract -- but no equivalent test exists for the SCOUT_* range/default text. | code-fix | M |
| docs/decisions/0019-env-var-validation-and-timeout-hierarchy.md:30 | Decision Outcome states '境界定数とデフォルトは src/tools/config.rs に集約し' (boundary constants and defaults are consolidated in config.rs), but max_retries' default (DEFAULT_MAX_RETRIES = 2) is not declared in config.rs. It lives in src/retry.rs:24 and is imported into config.rs:6, shared with src/github.rs:109, src/brave/client.rs:188, and src/slack/client.rs:152 for their own retry-attempt budgets. All timeout defaults (DEFAULT_FETCH/RESEARCH/SLACK/GITHUB_TIMEOUT_SECS) and all range constants (TIMEOUT_MIN_SECS, TIMEOUT_MAX_SECS, RETRIES_CAP) genuinely are colocated in config.rs, and the shared retries default is a defensible DRY choice given four call sites need the same value, but the DR's literal 'defaults are consolidated in config.rs' claim overstates this one field. A future edit to DEFAULT_MAX_RETRIES in retry.rs (tuning backend retry budgets) silently changes the SCOUT_MAX_RETRIES fallback and the --help default text without touching config.rs, contrary to the single-source-of-truth property the Consequences section credits ('境界定数とデフォルトが単一箇所に集約され、help・テスト・実装が乖離しない'). | dr-update | L |

### DR 0020: Search Default Output: One URL Per Line

Status: accepted

Result: 2 findings (H 0 / M 0 / L 2).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/lib.rs:47 | DR-0020 cites the empty-output guard as 'src/lib.rs:44-54', but write_output starts at line 47 (lines 42-46 are the unrelated enum Outcome&lt;T&gt; definition and a blank line) and the function body runs through line 58, not 54. The guard itself (output.is_empty() early return, lines 48-51) is semantically intact and matches the decision, so behavior has not drifted -- only the line-range citation in the DR is stale relative to the current file layout. | dr-update | L |
| src/tools/query_tests.rs:105 | DR-0020's Confirmation section says search_zero_results_returns_empty pins that 'stdout' becomes true empty output on 0 results. The test only asserts result.markdown() == "" on the in-memory CommandOutput (query_tests.rs:123), one layer above the actual stdout boundary. The real empty-output guard lives in write_output (src/lib.rs:48-51), which run() does call with output.into_markdown() on the non-JSON path (src/lib.rs:184-187), so production behavior matches the DR -- but no test (unit or integration) exercises write_output itself with an empty string, so the DR overstates what is pinned at the stdout layer versus what tests actually cover. | dr-update | L |

### DR 0021: CDP Chromium Launch Egress Flags

Status: accepted

Result: 4 findings (H 0 / M 1 / L 3).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/fetch/cdp/launch/cdp_launch_tests.rs:5 | DR-0021's Confirmation section claims `cdp_launch_tests.rs` pins 'the flag set' (フラグ集合を pin する), and frames the pin as the safeguard against the DR's own named risk ('chromium のバージョン更新でフラグ名/既定が変わると、無効化したはずの機能や bypass が復活しうる'). In code, T-F043 (line 5) asserts only 5 of the 9 hardening flags (webrtc, background-networking, DnsOverHttps, domain-reliability, no-pings) and T-201-8 (cdp_launch_tests.rs:23) asserts the 3 proxy flags. `--headless=new`, `--disable-extensions`, `--no-first-run`, and `--disable-default-apps` (build_launch_args, launch.rs:71,77-79) are part of build_launch_args's fixed set but are asserted by no test — deleting any of them from the vec today passes the full suite. This is a real gap between the DR's stated enforcement mechanism and what the test actually pins, not a cosmetic drift. | code-fix | M |
| docs/decisions/0021-cdp-chromium-launch-egress-flags.md:30 | Consequence section claims 'フラグが build_launch_args の単一箇所に集約され' (flags are consolidated in the single build_launch_args location) and '各フラグの根拠は本 ADR とコード近傍コメントに置き' (each flag's rationale lives in the ADR + adjacent code comments). In code, spawn_chromium_pgroup (src/fetch/cdp/launch.rs:156-161) adds `--remote-debugging-port=0` and `--user-data-dir={...}` directly at the spawn call site, outside build_launch_args, so not every arg passed to chromium is centralized there (these two are non-egress and already carry their own rationale comment at launch.rs:151-154, so this is likely benign given the DR's title scopes it to 'Egress Flags'). Separately, the code's per-flag doc comment (launch.rs:57-67) gives individual rationale only for the 3 proxy flags; the 9 hardening flags get one collective sentence in the ADR body and a generic 'See ADR-0021' pointer in code, not per-flag rationale as the Consequence literally claims. Worth a wording clarification in the DR rather than a functional fix. | dr-update | L |
| src/fetch/cdp/launch.rs:57 | Decision Outcome states each flag's rationale sits in both the ADR and a code-near comment ('各フラグの根拠は本ADRとコード近傍コメントに置き'). The doc comment at launch.rs:57-67 gives per-flag rationale for only the 3 proxy flags (proxy-server, proxy-bypass-list, disable-quic). The 9 hardening flags built at launch.rs:70-79 (--headless=new, --disable-webrtc, --disable-background-networking, --disable-features=DnsOverHttps, --disable-domain-reliability, --no-pings, --disable-extensions, --no-first-run, --disable-default-apps) have no code-near rationale; their per-flag purpose exists only in the DR's More Information table (0021-cdp-chromium-launch-egress-flags.md:76-87). A reader at the call site sees the flags with no explanation for 9 of 12. | code-fix | L |
| src/fetch/cdp/launch.rs:156 | The Good consequence claims flags are consolidated at the single build_launch_args site ('フラグがbuild_launch_argsの単一箇所に集約され'). --remote-debugging-port=0 and --user-data-dir=&lt;profile dir&gt; are instead set at the spawn_chromium_pgroup call site (launch.rs:156-160), outside build_launch_args. Neither is an egress-control flag named in the Decision Outcome's hardening/proxy groups (remote-debugging-port binds a local CDP listener, user-data-dir sets the profile path), so this is accepted as outside the DR's stated scope rather than a gap in the covered flag set. | accept | L |

### DR 0023: Proxy Egress Delegation for Fetch

Status: accepted

Result: 2 findings (H 0 / M 1 / L 1).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/tools/builder_tests.rs:695 | DR-0023's Confirmation (docs/decisions/0023-proxy-egress-delegation-for-fetch.md:53) states that src/tools/builder_tests.rs pins 'Proxied で fetch_http が proxy 経由になり guard を持たない' for the production build_default_clients wiring. Deletion-test trace: build_default_clients (src/tools/builder.rs:74) is called only twice in the whole crate -- builder.rs:120 inside from_env with the real detected egress, and builder.rs:150 inside for_test with EgressMode::Direct hard-coded (grep -rn build_default_clients confirms these are the only call sites). No test ever invokes it with EgressMode::Proxied. T-SB006 (this file, ~line 700-742) and T-F073 (src/fetch/fetch_page_tests.rs:172-216) each hand-build a client via Client::builder().proxy(Proxy::all(&proxy_url)).build() and inject it through with_fetch_http/with_egress -- their own doc comments say 'Mirror what production build_default_clients builds for Proxied.' If the real Proxied match arm at builder.rs:97-106 regressed (e.g. kept the SsrfResolver guard, or dropped .proxy()), every test the DR names would keep passing because none of them execute that branch. The DR's Confirmation therefore overstates what is pinned: the production Direct-vs-Proxied client-construction branch (the exact SSRF-guard-drop decision this ADR makes) has no regression test exercising the real code path. | code-fix | M |
| docs/decisions/0023-proxy-egress-delegation-for-fetch.md:53 | Confirmation section overstates two test claims. (1) It says T-FS022/023/024/027 pin '大文字優先' (uppercase-over-lowercase precedence), but no scenario in src/fetch/ssrf/egress_tests.rs sets HTTPS_PROXY and https_proxy simultaneously - T-FS022 only pins HTTPS_PROXY over HTTP_PROXY (proxy-type precedence, both uppercase), T-FS027 sets https_proxy alone with no competing uppercase var. src/fetch/ssrf.rs:44-46 says so directly in its own doc comment ('no test pins the case order - unverified'), which contradicts DR line 89's 'case 順は egress_tests の四シナリオが権威スペック'. (2) It credits src/tools/builder_tests.rs with pinning that build_default_clients produces a proxy-routed, guard-free fetch_http in Proxied mode, but grep confirms no test calls build_default_clients(&EgressMode::Proxied(...)) directly - T-SB006 (builder_tests.rs:695-744) and fetch_page_tests.rs:186 both hand-build a mirrored proxied client via with_fetch_http/with_egress and say so in their own doc comments ('mirrors what ScoutBuilder builds', 'this pins the builder -&gt; query wiring that produces it, not the client shape'). The Proxied branch of build_default_clients itself has no direct-call test. | dr-update | L |

### DR 0025: Span Newline Preserved Inside `<pre>`

Status: accepted

Result: 2 findings (H 0 / M 1 / L 1).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/fetch/converter.rs:125 | DR-0025's Decision Outcome states unconditionally that any &lt;span&gt; with a &lt;pre&gt; ancestor bypasses Handlers::fallback and returns walk_children's raw content, preserving embedded newlines as real line breaks. That holds only when span_handler (line 432) actually runs. When the &lt;pre&gt; also has a &lt;td&gt;/&lt;th&gt; ancestor, pre_handler's has_table_cell_ancestor branch (line 125, added in commit a4acf72 two days after DR-0025 was authored) short-circuits before per-child dispatch ever reaches span_handler: it reads the subtree through text_content/push_text_content (a direct DOM walk, not handler dispatch), wraps it with inline_code_span, and the caller then runs the whole cell result through normalize_cell_content (line 917-923), which folds every '\n' to a single space. Verified empirically with a scratch test: `<td><pre><span>line1\n</span>line2</pre></td>` renders as `` `line1 line2` `` (a space), not an embedded line break -- the opposite of the newline-preservation guarantee DR-0025 states as the rule. This fold is itself a deliberate, already-tested contract for plain-text &lt;pre&gt; content in table cells (T-FC093, `<pre>x\ny</pre>` -&gt; `` `x y` ``, since GFM table rows cannot contain a literal newline), so the code's behavior is correct and intentional, not a bug -- but DR-0025's Decision Outcome makes no carve-out for the table-cell case (unlike DR-0027, which explicitly lists table-cell/list-item/heading &lt;br&gt; as contract exceptions), and no test pins the span-specific combination (span nested inside a table-cell &lt;pre&gt;). | dr-update | M |
| docs/decisions/0025-span-newline-preserved-inside-pre.md:1 | DR-0025's Consequences section justifies skipping htmd's math-span exclusion by citing the wrong mechanism. It says the built-in math branch "requires attrs.len()==1 and a single Text child, so an element-child span structure never matches it anyway" — that gate description matches element_handler/span.rs:10-25 (a conversion branch reached only via Handlers::fallback), not dom_walker.rs:148-155's is_math_span (the actual fast-path exclusion, which checks attrs only, no child-structure requirement). More importantly, for a &lt;pre&gt;-nested math span (e.g. &lt;pre&gt;&lt;span class="math math-inline"&gt;x&lt;/span&gt;&lt;/pre&gt;), span_handler's has_pre_ancestor branch at src/fetch/converter.rs:433 returns via walk_children before Handlers::fallback is ever called, so span.rs's math branch never runs regardless of child structure — the DR's 'never matches anyway' claim is moot for the exact case it's invoked to justify, not true for the reason given. The resulting code behavior (no $-wrapping for any pre-nested span) is correct and intended; only this rationale sentence is factually wrong about which htmd mechanism does what. | dr-update | L |

### DR 0026: Pre Body Rebuilt from DOM, Not Reverse-Escaped from htmd's Walked Text

Status: accepted

Result: 5 findings (H 0 / M 2 / L 3).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| docs/decisions/0026-pre-body-rebuilt-from-dom.md:34 | Decision Outcome states pre_handler's non-code branch calls raw_pre_content, describing it as if that is the sole non-code path. Current pre_handler (src/fetch/converter.rs:115-147) has two non-code branches: the has_table_cell_ancestor branch (line 125-131, added later by commit a4acf72 / PR #415, after DR-0026 was accepted) does not call raw_pre_content at all. It instead reads the subtree through a separate function, text_content/push_text_content (converter.rs:485-507), which recurses directly over node.children and never invokes Handlers::handle on Element children. This third code path, and its own dependency on the walk_children sibling-merge side effect the DR's Trade-offs/Reassessment-Triggers sections discuss only in terms of raw_pre_content, is absent from the DR entirely. Functionally the branch does not reintroduce the escaping bug DR-0026 fixed (it never triggers htmd's escape_pre_text_if_needed since it bypasses handler dispatch), so this is not a runtime regression, but a maintainer relying on DR-0026's Reassessment Triggers (e.g. 'htmd changes can_combine merge conditions') to judge blast radius would miss that the table-cell branch shares the same fragile assumption and needs the same re-check. | dr-update | M |
| src/fetch/converter.rs:125 | DR-0026's Decision Outcome describes pre_handler as splitting into exactly two branches: a code-child branch and a 'non-code' branch that calls raw_pre_content. Commit a4acf72 (2026-08-16, two days after the DR was accepted) inserted a third branch ahead of both: has_table_cell_ancestor, which never existed at DR-0026's own commit (670b72c). This branch intercepts pre elements inside &lt;td&gt;/&lt;th&gt; regardless of whether they have a &lt;code&gt; child, using text_content/inline_code_span instead of raw_pre_content, and consumes result.markdown_translated from the top-of-function walk_children call the DR describes as being for raw_pre_content's benefit alone ('呼び出しの直前に...その戻り値の文字列は使わずに捨てる'). The decision's substance (no reverse-escaping of walked text) still holds for this branch since it also reads the DOM directly, but the DR's stated branch model no longer matches pre_handler's actual structure, and the DR's Reassessment Triggers don't mention this branch at all. | dr-update | M |
| src/fetch/converter.rs:366 | The raw_pre_content doc comment cites `can_combine` at htmd-0.5.5/src/dom_walker.rs:243-297. Reading the vendored htmd-0.5.5 source (~/.cargo/registry/.../htmd-0.5.5/src/dom_walker.rs), `fn can_combine` actually spans lines 250-308, matching DR-0026's own citation (dom_walker.rs:250-311, line 17 of the DR) much more closely. The code comment's range starts 7 lines early and ends 11 lines short, cutting off before line 301 (`attrs1 == attrs2`), the very condition the comment is pointing readers to. | code-fix | L |
| src/fetch/converter.rs:2046 | The T-FC034 (line 2046), T-FC035 (line 2074), and T-FC036 (line ~2100-2105) doc comments narrate the deleted opens_with_escaped_fence_char / front-anchored content.strip_prefix('\\') mechanism in the present tense ('today's front-anchored strip never reaches it', 'so `content.strip_prefix` fails silently', 'The bug must not disappear once recursion is involved'), as if that machinery still runs. DR-0026's Decision Outcome states this position-based reverse-escaping is deleted entirely because raw_pre_content reads Text child `contents` straight off the DOM (confirmed: grep finds zero function-definition hits for opens_with_escaped_fence_char, only these stale comment references). The assertions in these three tests are correct (they check the backtick survives unescaped), but the explanatory prose describes why the pre-DR-0026 implementation would have failed, not why the current DOM-read implementation succeeds, which is confusing for a reader relying on the comment to understand current behavior. | code-fix | L |
| src/fetch/converter.rs:2149 | The T-FC038 test doc comment states raw_pre_content 'appends each Element child's own converted content directly (`content.push_str(&res.content)`) with no separator logic of its own.' This is false: line 381 calls push_element_content(&mut content, &res.content), which caps the newline run at the junction. This contradicts both the actual code and DR-0026's own Consequences text ('改行上限は push_element_content として明示的に再実装した (T-FC038)'), which credits this exact test with pinning that reimplementation. | code-fix | L |

### DR 0027: Fetch Body Line-Break Handling: Paragraph Folding as Contract, `<br>`/`<pre>` as the Named Exceptions

Status: accepted

Result: 2 findings (H 0 / M 1 / L 1).

| File:Line | Description | Direction | Priority |
| --- | --- | --- | --- |
| src/fetch/converter.rs:125 | DR-0027's Decision Outcome states contract 3 unconditionally: "`<pre>` の中身は畳まず、原文の改行をそのまま残る", and scopes the only named exceptions (表セル・リスト項目・見出しの中の `<br>`) to `<br>`, not to `<pre>`. Empirically verified (scratch test, reverted): a bare `<pre>` with a text or `<code>` child nested inside a `<td>`/`<th>` takes the `has_table_cell_ancestor` branch at converter.rs:125-131, which reads `text_content` (raw newlines intact) and wraps it as an inline code span; `extract_row_cells` (converter.rs:886-902) then runs that through `normalize_cell_content` (converter.rs:917-923), which unconditionally does `.replace('\n', " ")`. `<table><tr><td><pre>line1\nline2</pre></td></tr></table>` converts to a cell containing `` `line1 line2` ``, i.e. contract 3's 'original newlines survive' claim is false inside a table cell. This is not a fresh regression: `T-FC025` (`td_pre_does_not_split_the_table_row`, converter.rs:1126-1160) already pins this exact `normalize_cell_content` fold for `<pre><code>` inside `<td>`, predating DR-0027's numbering (T-FC025 &lt; T-FC041). DR-0027 was written without cross-referencing this pre-existing, already-tested behavior, so its 3-form contract text is incomplete relative to code that was already correct and already covered by a test. The code behavior is structurally forced (GFM table cells cannot hold a literal newline without breaking the row, same structural reason the DR already gives for the `<br>`-in-cell exception), so the code is the correct current contract and the DR text is stale/incomplete, not the other way around. | dr-update | M |
| docs/decisions/0027-fetch-body-line-break-handling.md:34 | DR-0027 の Decision Outcome は改行の扱いを3契約(畳む/br hard break/pre非畳み)+3つの named exception (td/li/heading内のbr)に固定し、Confirmation は inline code の空白保持(T-FC044)もこの契約の一部として引き込んでいる。しかし src/fetch/converter.rs:1602 の T-FC054 (span_inside_inline_code_outside_pre_loses_the_newline_entirely) が示す通り、&lt;code&gt;&lt;span&gt;line1\n&lt;/span&gt;line2&lt;/code&gt; という inline code 内に &lt;span&gt; が入れ子になった形では、htmd 組み込みの span handler の trim_matches('\n') が code handler の畳み処理より先に走り、改行が空白1個へ畳まれる(契約1)のではなく1文字も残さず消える("line1line2")。これは契約1(空白1個へ畳む)、契約2/3(hard break/非畳み)、3つの named exception (表セル・リスト項目・見出しの中の br)のいずれにも当たらない第4の形で、DR がまさに避けようとした「1段落1行」型の偽の単純化と同じパターンが inline code の隅で再発している。コード自体は htmd の標準動作でありバグではないため、direction は dr-update: DR の Decision Outcome または Confirmation に、inline code 内に &lt;span&gt; が入れ子になったときの改行消失を4つ目の named limit として追記するのが妥当。テストが既に green で固定されているため実害は小さくコメント/ドキュメント整合性の問題にとどまる。 | dr-update | L |

DRs 0022, 0024: no drift.

## External DR Dependencies

| File:Line | External DR ref | Recommended action |
| --- | --- | --- |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:1 | DR-0060 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:5 | DR-0060 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:7 | DR-0060 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:12 | DR-0060 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:16 | DR-0060 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:74 | DR-0060 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:82 | DR-0060 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:86 | DR-0060 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:98 | DR-0060 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-053219-adr-drift.md:203 | DR-0060 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-053219-adr-drift.md:207 | DR-0060 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-080315-adr-drift.md:93 | DR-0060 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-080315-adr-drift.md:94 | DR-0060 | Promote to local DR or supersede locally |
| docs/audit/2026-05-14-adr-drift.md:68 | DR-0060 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:88 | DR-0061 | Promote to local DR or supersede locally |
| .claude/workspace/research/2026-05-07-adr-0060-scout-cli-gap.md:98 | DR-0061 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-053219-adr-drift.md:203 | DR-0061 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-053219-adr-drift.md:207 | DR-0061 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-080315-adr-drift.md:94 | DR-0061 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-18/idr-01.md:114 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-17/idr-01.md:165 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-17/idr-01.md:167 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-17/idr-01.md:180 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-03.md:1 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-03.md:7 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-03.md:86 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-03.md:94 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-03.md:460 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-03.md:499 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-03.md:534 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:1 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:7 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:115 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:126 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:156 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:160 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:181 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:187 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:193 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:199 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:213 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-04.md:236 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-06.md:1 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-06.md:52 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-06.md:64 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-05.md:25 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-05.md:72 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-02.md:19 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-02.md:29 | DR-0065 | Promote to local DR or supersede locally |
| .claude/workspace/planning/2026-05-07/idr-02.md:30 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:47 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:51 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:52 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:53 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:54 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:55 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:58 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:59 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:61 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:65 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:70 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:76 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:77 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:81 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:83 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:87 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:93 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:95 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift.md:97 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-undocumented-decisions-part2.md:103 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-053219-adr-drift.md:111 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-053219-adr-drift.md:161 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-053219-adr-drift.md:203 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-053219-adr-drift.md:208 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:31 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:46 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:47 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:50 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:102 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:105 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:113 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:115 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:123 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:140 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-19-undocumented-decisions.md:182 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift-postimpl.md:15 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift-postimpl.md:44 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift-postimpl.md:46 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift-postimpl.md:95 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift-postimpl.md:96 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift-postimpl.md:99 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift-postimpl.md:109 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-13-adr-drift-postimpl.md:127 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-080315-adr-drift.md:92 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-14-adr-drift.md:17 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-14-adr-drift.md:40 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-05-14-adr-drift.md:66 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-020601-adr-gaps.md:76 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-020601-adr-gaps.md:91 | DR-0065 | Promote to local DR or supersede locally |
| docs/audit/2026-06-24-020601-adr-gaps.md:189 | DR-0065 | Promote to local DR or supersede locally |

## Follow-up Issue Candidates

- [ ] DR 0002 drift at docs/decisions/0002-adopt-sysexitsh-exit-code-convention-for-cli.md:38: DR-0002's decision table (line 38: 'EX_IOERR | I/O エラー (network, disk, external tool failure)') and detail table (line 105: '74 | network error、TLS error、headless browser CDP error') both state that network/TLS-level failures map to exit 74. The implementation deliberately routes them elsewhere: src/classify.rs:87-98 (`Classification::from_reqwest`) sends a `reqwest::Error` to `Timeout` (124) if ...
- [ ] DR 0005 drift at src/fetch/converter.rs:25: DR-0005's Confirmation section pins the research JSON schema as `data.fetched_pages[i] = {url, markdown, used_raw_fallback}`. In code, `FetchResult.used_raw_fallback` (and `decode_uncertain`) carry `#[serde(skip_serializing)]` (src/fetch/converter.rs:24-30), so `data.fetched_pages[i]` actually serializes to `{url, markdown}` only. The flag is instead surfaced via `degraded_reasons`/notes ...
- [ ] DR 0008 drift at docs/decisions/0008-trait-injection-and-scoutbuilder-for-test-seam.md:40: Decision Outcome states clock/rng/token_source "are forwarded into GitHubClient on first Scout::github() call (OnceCell lazy init)", framing GitHubClient as the single destination via one lazy mechanism. Current code fans clock/rng out to three destinations with different timing: GitHubClient lazily (src/tools.rs:222-231, matches DR), BraveClient eagerly inside ScoutBuilder::build() ...
- [ ] DR 0009 drift at src/tools/builder.rs:104: build_default_clients(egress: &EgressMode) hardcodes Arc::new(SsrfResolver::new(TokioDnsResolver)) as the connect-time DNS resolver wired into fetch_http for EgressMode::Direct, and is called from both from_env and for_test before with_dns is ever applied. Scout.dns / ScoutBuilder::with_dns (DR-0009's seam) only reaches the pre-flight ssrf_check resolver via fetch_page/self.dns.clone(); it never ...
- [ ] DR 0010 drift at src/github/types.rs:15: DR-0010 Rule 4 requires every array field under the envelope's `data` key to serialize as `[]`, never `null`, and explicitly forbids `Option<Vec<T>>` for this reason. `RepoInfo::topics` is typed `Option<Vec<String>>` and derives `Serialize` with no `skip_serializing_if`/normalization. This same struct is embedded verbatim into the `repo-overview` JSON envelope at src/tools/repo.rs:212 ...
- [ ] DR 0011 drift at src/fetch.rs:132: The same semantic condition (upstream response exceeds scout's byte cap) maps to different ErrorCodes across backends: FetchError::TooLarge -> DataError/65 (src/fetch.rs:132), but GitHubError::ResponseTooLarge -> Internal/70 (src/github/errors.rs:110, explicitly pinned 'peer to Decode' by T-GHC010) and BraveError::ResponseTooLarge -> Internal/70 (src/brave/client.rs:108). Both are non-retryable ...
- [ ] DR 0011 drift at src/tools/query.rs:20: DR-0011 priority 3 names 'search 0 件' as a NotFound (exit 66) condition, but `search()` returns a success envelope with empty `sources` and exit 0 for zero Brave results (pinned by T-TS027 in src/tools/query_tests.rs:105, and by T-TS0xx research_json_zero_results_returns_empty_arrays for research). No production path constructs ErrorCode::NotFound for a zero-result search; ...
- [ ] DR 0014 drift at docs/decisions/0014-output-injection-defense-for-agent-consumers.md:34: Decision Outcome's opening sentence says neutralization is concentrated in pub(crate) functions in `src/markdown.rs` and `src/fetch/converter.rs`. Since the 2026-08-03 yaml-helper-module-move refactor, the YAML/marker neutralization primitives (`neutralize_yaml_markers`, `neutralize_yaml_markers_outside_fences`, `write_yaml_str`, `escape_yaml`) live in `src/yaml.rs`, not `src/fetch/converter.rs`. ...
- [ ] DR 0014 drift at src/markdown.rs:225: The DR's 中和点 table row for `shift_headings` (docs/decisions/0014-...md:99) states its guard as only '(code fence 内・非 ATX は skip)'. The function also rewrites setext headings to ATX (src/markdown.rs:258-266) and skips a leading frontmatter block via `frontmatter_len` (src/markdown.rs:113-121) specifically because an unshifted closing `---` was being read as a setext underline and consumed, ...
- [ ] DR 0014 drift at src/yaml.rs:112: DR-0014's Decision Outcome (docs/decisions/0014-...md:36) and Consequences (line 48) describe fetch's fence fail-closed switch as the whole story, and the 中和点 table (line 93-106) and Confirmation section (line 59) enumerate only escape_yaml/neutralize_yaml_markers/neutralize_yaml_markers_outside_fences/format_readme_section/format_tree/format_file_content. Neither section mentions ...
- [ ] DR 0017 drift at src/lib.rs:197: Under `--json`, the `Outcome::Interrupted` arm in `run()` does `eprintln!("error: interrupted ({sig})")` unconditionally, ignoring `json_mode`. Every other error-emitting arm in the same match (`emit_error` at line 175/196, `write_failure_line` at line 191, `handle_parse_error`) branches on `json_mode` and emits a JSON envelope. T-W004's doc comment (line 432-435) states the contract explicitly: ...
- [ ] DR 0018 drift at docs/decisions/0018-github-token-resolution-precedence.md:48: Confirmation closes with '(この注入を入れるまで) ... どの分岐を走るかが、テストを回すマシンに gh が入っているかに依存していた' stated as resolved by the spawn_gh injection. That injection only reaches src/token_source.rs's own unit tests (T-TOK001-006). ScoutBuilder::for_test() (src/tools/builder.rs:157) still defaults token_source to the production GhCliSource rather than StaticTokenSource, so Scout::github() lazy-init tests built on ...
- [ ] DR 0019 drift at src/tools/config.rs:168: DR-0019's Decision Outcome states the outer/inner timeout hierarchy is preserved even under `SCOUT_*` override ('SCOUT_* override 時もこの大小を崩さない値域 (≤600s) に収める'), but `parse_timeout` validates `github_timeout` against the same shared `TIMEOUT_MIN_SECS..=TIMEOUT_MAX_SECS` (1..=600) range used for fetch/research/slack, with no floor tied to the inner per-request timeouts. Setting ...
